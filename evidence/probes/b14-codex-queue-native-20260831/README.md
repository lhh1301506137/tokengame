# B14：Codex 同一任务自动发言的实测摘录

这是可由 Git 复核的最小去敏事实包，不是产品交付或清理通过凭证。完整原始输出保留在被忽略的 `artifacts/b14-native-readiness-authorized-20260831/`；这里只保存本批原任务的控制通知、游戏工具输入输出、权威去敏事件、公开 DOM 观察及清理结果，不保存隐藏推理、底牌值或连接凭据。

本批依据 `DEC-20260831-002`、`DEC-20260831-003`：一个既有 Codex 游戏任务，模型与推理强度不覆盖；一次只读准备，三次不同来源的单次 queue 通知。两次来源在未开手等待区，第三次在第一手翻牌前。三次都发生一次真实 `ai.start`、一次 `ai.resolve`，权威各接受一次公开回复；A 不需要在来源事件后再点击或发送提示。后两次在 A/B 两页实际看到同文座位 AI 气泡。第一例采样晚于气泡寿命，只见公共历史。

三次来源到权威公开分别为 43,857、46,785、43,660 毫秒。它们不是纯推理耗时，不是稳定性能统计。牌局中样本已从翻牌前进入翻牌；权威返回延迟标注，但修复前 UI 漏显。之后的映射修复及脚本浏览器回归另见 `artifacts/b14-late-marker-fix-20260831/`，不能倒写成这三次原生实测已显示标签。

资源：本席权限已撤销，旧凭据经独立 MCP 被拒；临时项目级配置已移除，两页已关闭；本批 beta 经 IPC 真实退出 0，捕获尾行、写入及关闭回执完整。最后的失效凭据文件和专用 MCP 进程清理被工具策略在命令执行前拒绝，未绕过。因清理门禁未闭合，本包不得独自支持架构或产品节点完成；默认 `proactive_wake_verified` 不变，Claude 仍未测。

`native-tools.json` 是原任务本地日志的结构化摘录，而不是任务总结转述。每条保留来源行号；模型上下文整体移除。原始日志还会增长，所以 SHA-256 表示摘录时快照，不能与以后追加的整个日志直接比较。`manifest.json` 校验本包实际文件字节；哈希只是本地完整性信息，不是平台签名。

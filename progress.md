Original prompt: 继续（恢复并实现 TG-L3-CODEX-BRIDGE-SPIKE 本地聚焦探针）

# TokenGame 开发进度

## 2026-09-03 — B29（公开协作前检查，等待远端与许可证确认）

- 将项目对外定位收敛为“实验性本地 Alpha”：正常无限注德州仍由真人操作，每席会话 AI 只读取本席获准上下文并公开发言或沉默；异地联机、完整朋友内测、Claude 接入和 Codex 内嵌 UI 均明确列为未完成。新增中文贡献指南、PR 模板和 Linux/Windows Node 22 CI；`.gitignore`补入浏览器临时目录与本人 AI 连接文件，未改牌局或模型权限语义。
- 首轮完整`npm run gate`基础测试1356/1356通过，但668条变异中`wake-ui-target-hidden`存活。根因是变异向机器采样对象加入值为`undefined`的`thread_id`自有属性，既有测试只检查`JSON.stringify`，序列化会静默省略该属性；产品代码本身仍拒绝服务端返回目标UUID。补充对象自有属性断言后，聚焦Node 72/72，定向变异1/1被杀。
- 修正后脚本浏览器验收46/46、0 error，桌面固定目标与320px等待回执截图已目检；完整发布门最终为Node 1356/1356、变异668/668被杀、0存活、0跳过。仓库外证据位于`H:/tokengold/.codex/b29-public-release-ui-qa-20260903/`，不纳入提交。
- 当前尚未创建或配置Git远端，也没有许可证；因此本批在远端名称、可见性、许可证以及是否保留含本机路径/任务UUID的既有开发历史经用户确认前不执行push。上述内部元数据扫描未发现凭据，但公开后会暴露开发过程，不能与“无秘密”混写成“无隐私信息”。

## 2026-09-02 — B28（空闲游戏任务交接合同本地闭合）

- B27证明绝对`cwd`和项目工具已恢复，但把唯一通知固定到仍在执行开发回复的同一任务只得到尝试/接收/权威结清1/1/0。B28没有重试通知，而是修正可控入口缺口：managed横幅、固定目标页面说明、每窗本人确认、项目Skill、根/插件README和操作文档统一要求先结束启动回复，让游戏任务保持空闲；queue已接收不等于模型开始或权威结清。
- 页面新增的确认只是一项真人声明，不是宿主空闲遥测或技术门禁；没有新增调度器、任务创建、模型API或后台循环，也没有改变L0–L2语义。
- 新增/扩展真实beta启动横幅、文档/Skill一致性及固定目标浏览器断言。聚焦Node首轮18/19因同义措辞不满足精确合同，修正后19/19、1833.6463ms；脚本浏览器46/46、20729.272ms、0 browser error，四上下文/浏览器/夹具6项清理通过，桌面和320px截图已目检；完整`npm test`为1356/1356、79933.5359ms。
- Skill普通校验首次因Windows Python按GBK读取UTF-8文件失败；同一校验器使用`python -X utf8`后返回`Skill is valid!`，载体问题单列。相关JavaScript语法通过；`git diff --check`仅有既有`plugin.json`行尾提示。没有运行变异门禁，项目无lint/type-check脚本。
- 持久化恢复文档后，同一聚焦套件再次19/19、1705.2711ms，UTF-8 Skill校验再次通过；这是收尾一致性复核，不另计产品覆盖。
- 本批实际0通知、0queue、0原生模型回合、0权威评估；最终活动槽不存在、7802无监听。仓库外浏览器证据在`H:/tokengold/.codex/b28-idle-task-guidance-20260902-a1/`。裁决为`idle_game_task_handoff_guidance_implemented_and_locally_verified`，见`REVIEW-LOG.md#b28-idle-game-task-handoff`。
- 下一步不是继续拆入口说明或重试当前开发任务，而是真实本地朋友组合：各自游戏任务启动beta后结束回复、保持空闲，再由Web牌桌逐席授权和本人开窗；仍以权威start和唯一terminal验收。当前不自动创建任务、不开放远程或第二模型，`proactive_wake_verified=false`且可玩性门禁继续开放。

## 2026-09-01 — B27（绝对 `cwd` 工具恢复；当前活动任务通知 1/1/0）

- 用户完成`DEC-20260901-002`内的第二次手动Codex重启。当前任务实际且只加载项目`mcp__tokengame_project__tokengame_table`；无活动连接时直接`view.projection`返回`model_connection_unavailable`，激活后同一只读调用成功。绝对`cwd`的配置迁移、重启与工具加载现已全部闭合，不再重复迁移或重启。
- 从仓库根运行稳定入口后，回环beta在7802启动。两个隔离headed Chromium完成A建房、B加入和公开范围确认，牌桌停在第0手等待区。A确认本席AI权限、下载并激活一份新连接，固定目标页面不显示UUID；本人开启最多1次、60秒窗口，B只发送一条合成公开来源。
- 唯一窗口最终为尝试1、接收1、权威结清0，在60.003秒以`max_duration`停止，`cleanup_ok=true`、`failure_code=null`、`native_turn_state=unknown`。固定目标正是仍处于本开发回合`inProgress`的同一任务；窗口内0个可重入新模型回合、0次`ai.start/resolve`、0条AI气泡。精确排队/延后执行语义unknown；没有补发或手工抢待办，不能把queue接收写成主动AI成功，也不能把0结清归因扑克或模型。
- A/B两页最终状态一致，控制台0 error、0 warning，截图与`render_game_to_text`已直接目检。本批没有产品源码修改，也没有补跑Node、变异或脚本浏览器套件。
- 收尾已完成服务端撤权、`connection:clear`、撤销后原生失败关闭、两浏览器关闭和beta停止；活动槽不存在、7802无监听。外层PTY exit1只表示Ctrl+C。新下载已撤权、未读、未删；仓库精确模式累计7份、1165字节，继续由真人决定删除。
- B27裁决为`absolute_cwd_host_tool_recovered_fixed_target_notification_accepted_self_target_busy_unresolved`。下一步不在正在运行的开发任务上重试；转为空闲游戏任务的本地朋友组合准备，并仍以权威start与唯一terminal验收。事实见`REVIEW-LOG.md#b27-absolute-cwd-post-restart-fixed-target`。

## 2026-09-01 — B26（真实绝对 `cwd` 已迁移，等待第二次手动重启）

- 用户以`DEC-20260901-002`明确授权只把`H:/tokengold/.codex/config.toml`中TokenGame唯一托管块的`cwd`恢复为`H:/tokengold/tokengame`，其他配置不变，并由用户再次手动重启Codex。
- 迁移前确认目标为普通非链接文件、291字节、托管标记1/1、相对`cwd`存在；完整SHA-256为`4C48CABF471B24D7ADEFEAD8B66B78553EF2A0CD3BB526AB1E0897EFFBA5655F`，托管块外SHA-256为`01BA4719C80B6FE911B091A7C05124B64EEECE964E09C058EF8F9805DACA546B`。
- 在当前任务从仓库根执行一次`npm run codex:play -- "H:\tokengold"`，exit 0且只输出去敏重启提示；命令在beta前停止。迁移后目标为304字节，唯一托管块精确为绝对`cwd`，旧相对值消失；完整SHA-256为`63EB0B67B285B59E30692B50795546ABD87B6D59403AD9E9183E16117D9C769E`，块外SHA-256与迁移前完全一致。配置无任务UUID或绝对Codex可执行路径，7802无监听。
- 本批未补跑Node、变异或浏览器测试，实际0通知、0模型、0queue、0权威评估、0浏览器。第二次手动重启及重启后`tokengame_table`直接就绪仍未执行，不能把配置正确写成宿主恢复。
- B26裁决为`absolute_cwd_real_config_migrated_waiting_for_second_manual_restart`。当前唯一恢复点是用户手动重启Codex、回到同一任务说“继续”；Primary先核对工具就绪，随后才可继续原定一次固定目标、最多1次通知的有界原生验收。事实见`REVIEW-LOG.md#b26-absolute-cwd-real-config-migration`。

## 2026-09-01 — B25（相对 `cwd` 宿主失败，绝对路径本地修复）

- B24授权的一次真人手动重启已由进程时刻直接确认：项目配置写于22:17:10，新的Codex/ChatGPT进程分别创建于22:23:40/22:23:49。`codex mcp list --json`能列出`tokengame_project`及相对`cwd = "tokengame"`，但当前任务在beta启动前后都没有`tokengame_table`。同一任务的既有记录显示旧canonical绝对仓库`cwd`时曾实际调用该工具并得到缺活动槽的受限错误；因此配置识别不能替代工具加载，相对`cwd`假设已被直接反证。
- 第二次`npm run codex:play -- "H:\tokengold"`成功启动回环beta。两份隔离Chromium完成A建房、B加入和公开范围确认；A页面确认本席AI权限、下载并激活连接，直接目检固定目标不显示任务UUID、最多1次/60秒。当前任务仍无工具，所以没有开通知窗、没有Ready/开手/公开来源；实际0通知、0原生模型回合、0queue、0权威评估。
- 清理按页面服务端撤权→`connection:clear`→关闭两浏览器→停止beta执行；页面回执明确旧文件不能再请求，本地活动槽不存在，7802无监听。外层PTY在SIGINT后exit1与beta内部端口/定时器清理分开记录。新增一份166字节已撤权、未读、未删的下载文件，连同历史五份等待真人处理。
- 本地生成器已恢复canonical绝对仓库`cwd`；旧相对受管块会原子迁移，块外用户配置逐字节保留，首次变化仍只提示重启，稳定CLI/UI输出不回显本机路径或任务ID。红测20/23（3个预期失败）后聚焦34/34、定向变异1/1被杀、Node语法通过。独立复核补齐生成配置会暴露本机目录布局、禁止分享/提交及仓库模板例外的四份文档断言；初轮23/24暴露断言换行误报，修正后24/24（629.6933ms）、同一变异1/1（906ms）、语法和范围diff检查通过。真实`H:/tokengold/.codex/config.toml`没有再次修改，也没有第二次重启或通知；这些动作需要新的明确授权。
- B25裁决为`codex_desktop_relative_mcp_cwd_runtime_load_failed_before_notification`。`proactive_wake_verified=false`且`TG-EU-PLAYABILITY-GATE`继续blocked；唯一恢复点是先获准恢复真实父项目的canonical绝对仓库`cwd`并第二次手动重启，再继续原定最多1次的固定目标原生验收。事实见`REVIEW-LOG.md#b25-relative-cwd-host-failure`。

## 2026-09-01 — B24（项目配置迁移完成，等待手动重启）

- 用户以`DEC-20260901-001`明确授权项目级TokenGame托管配置迁移及一次Codex重启。迁移前确认`CODEX_THREAD_ID`格式有效、Codex应用入口可解析、目标配置只有一个TokenGame托管块且7802无监听。
- 在当前任务从仓库根执行一次`npm run codex:play -- "H:\tokengold"`，exit 0并只输出“TokenGame 已配置，请重启目标 Codex 任务后重跑。”；命令在beta启动前停止，0监听、0原生模型输入、0queue。
- `H:/tokengold/.codex/config.toml`现为`cwd = "tokengame"`。托管块外SHA-256迁移前后均为`E3B0C442...B855`（目标文件当前无块外内容），唯一标记块保持1/1；配置不含绝对仓库路径、任务UUID或绝对Codex可执行路径，7802继续无监听。
- 该检查直接证明配置字节迁移成功，但不能证明当前仍在运行的宿主已重载配置或能解析相对`cwd`。唯一下一步是用户手动重启Codex并回到本任务；随后重跑同一命令，才进行一次回环、固定目标、最多1次通知的有界原生验收。`proactive_wake_verified=false`且`TG-EU-PLAYABILITY-GATE`继续blocked。事实见`REVIEW-LOG.md#b24-project-config-migration`。

## 2026-09-01 — B23（固定任务去敏与 Codex 当前任务一键本地入口）

- 固定目标叶让`createCodexQueueSender`只在服务端选择预配置任务：缺省任务只对提供`selectThread`的固定sender开放，显式精确匹配的合法ID被规范化；在会话/API入口，畸形`thread_id`以`invalid_field`拒绝，合法但外来的ID以`wake_thread_not_authorized`拒绝。没有该能力的旧/自定义queue继续要求手填任务UUID。轮询及start/status/stop响应不再投影`thread_id`，只给无秘密布尔`target_configured`；固定模式页面隐藏并禁用UUID输入，请求体不含`thread_id`。实现聚焦184/184、初始直接变异14/14；独立复核再跑184/184，并修复失效变异锚点与错误响应可能回显目标的缺口，限定变异11/11。
- 脚本浏览器先以旧夹具运行，并在累计7 checks时按预期失败；显式增加`fixedTarget`测试能力后最终44/44、约11.40秒，0 console/page error。固定模式4次start请求均无`thread_id`，旧手填兼容模式1次仍携带测试UUID，45个可见响应均未出现已知任务ID；截图由主线程目检为固定目标提示且无UUID。该批只用回环夹具，0原生模型、0原生queue；浏览器上下文和端口均已清理。
- 新增首选入口`npm run codex:play -- "<当前 Codex 项目根绝对路径>"`。它只读取、校验并小写化`CODEX_THREAD_ID`，不读取、比较、回退、输出或持久化`CODEX_SESSION_ID`；严格按项目canonical/仓库包含关系→thread→可执行文件的顺序完成只读前置，再原子配置。显式`TOKENGAME_CODEX_EXECUTABLE`优先且独占；无显式值时只在Windows按PATH顺序取第一个实际`codex.exe`，该第一候选必须是非符号链接普通文件且canonical位于canonical `%LOCALAPPDATA%/OpenAI/Codex/bin/<hex>/codex.exe`，不可信即拒绝、不跳后项；非Windows必须显式提供。
- 受管MCP的`cwd`现写为相对当前Codex项目根的仓库路径，不持久化绝对repository。配置发生变化时入口只给去敏重启提示并成功停止；配置未变化时才在同一进程调用一次beta `main({env})`。启动环境强制回环、进程内内核、无模型adapter、无receipt并过滤调用方全部`TOKENGAME_*`和session键；不改变模型/推理，不自动开启通知窗，不枚举任务，不全局安装或远程监听。稳定输出与配置不含任务/会话UUID、可执行路径或绝对repository；短命queue子进程参数及同系统账户可见的进程元数据仍可能包含任务ID。
- 一键入口红测为模块不存在导致0/1；首轮实现后新叶9/9、直接既有配置5/5、变异9/9。独立审查发现并修复beta启动失败可能被收尾覆盖为exit 0、绝对`cwd`持久化与畸形Windows路径矩阵，最终新叶9/9（约0.37秒）、beta/config/lifecycle 88/88（约10.55秒）、专用变异15/15（约9.09秒），Node语法与diff检查通过。一键入口子叶没有启动监听、模型、浏览器或原生任务；固定目标脚本浏览器另按上一条单列，真实`H:/tokengold/.codex`未改。
- B23裁决只把本机同一任务接入从页面手填UUID及四变量降到一条当前任务命令；它不证明MCP已由当前宿主加载、相对`cwd`已由宿主解析、原生queue/公开/实时性、第二真实AI席或朋友异地联机。`proactive_wake_verified=false`且`TG-EU-PLAYABILITY-GATE`继续blocked。唯一恢复点是等待用户明确授权项目配置迁移与宿主重启；获准后在目标Codex任务内首次运行新命令，预计把既有绝对`cwd`受管块迁为相对值并要求重启，重启后再运行并做一次回环、固定目标、最多1次的有界原生验收。唯一裁决见`REVIEW-LOG.md#b23-fixed-target-codex-play`。

## 2026-09-01 — B22（managed fast-path 单次原生对照：同手 silent 结清）

- 在B21相同的既有专用任务上启动随机回环beta端口53952，两份隔离headed浏览器确认公开范围并以A/B同桌；A新连接激活后，开窗前A席AI为`ON/IDLE`、无pending intent或active turn，专用任务为idle，生命周期文件只有header。本人先开启最多1次/120秒窗口，再让两席Ready；窗口开启后没有追加玩家公开消息。
- 首次beta启动因B22证据目录尚未创建而在监听、浏览器、queue和模型输入之前以`ai_receipt_open_failed`退出1；补建明确的本批目录后才开始唯一真实样本。该前置事实来自启动命令回执，不在随后成功样本的生命周期文件或页面产物中；该载体准备失误不计模型样本，也没有因此增加通知额度。
- 整批严格只有1次原生queue，无第二来源、重试或第二模型。专用任务turn `01a05cc2-4fda-7df3-8cbd-0e9fc9fe49ff`由任务接口记录为`startedAt=1788262633s`、24.340秒完成；queue精确投递时刻以及任务开始的毫秒部分均`unknown`。权威第1手开始于`1788262631524ms`，`ai.start`于`1788262645233ms`，`silent`终态于`1788262655050ms`，行动截止为`1788262661526ms`，第2手开始于`1788262664728ms`。
- 同口径时延为：HAND1→task约1.476秒、task→start约12.233秒（两段受任务接口仅秒级精度限制），HAND1→start精确13.709秒，start→terminal精确9.817秒；终态在行动截止前6.476秒、在第2手前9.678秒结清。相对B21约9.7秒/约14.0秒/23.701秒/10.781秒，名义上分别缩短约8.2秒、约1.8秒、9.992秒、0.964秒；这只是单次对照，且HAND1仍只是缺失精确source行时的早期窗口代理，不能单独归因fast-path或外推SLA。
- 最终窗口因`max_notifications`停止，尝试/接收/权威结清为1/1/1、`cleanup_ok=true`、`failure_code=null`；权威决策为`silent`、`reason=null`，两页AI气泡均为0，属于同手合法终态而不是公开回复。任务读取接口的最新turn `items=[]`，因此首次可见工具调用是否确为`ai.start`、是否存在前置宿主进度或其他工具均`unknown`；权威只能证明本次确有一次`ai.start`及对应唯一终态，不能补写载体未返回的调用顺序。
- 去敏记录捕获完整、0 dropped，内部关停回执为`normal_close`且`write_acknowledged/close_succeeded/run_complete=true`；离线摘要因没有行动窗口source行而为`partial`，但确认1个start、1个silent和9.817秒start→terminal。最终两页截图已视觉核对，均显示第8手且0条可见AI气泡；同次浏览器会话的只读console查询回执为两页各0 error/0 warning，但输出目录没有单独的console日志，截图本身不证明该计数。本批不重跑Node、变异、脚本浏览器或全量测试，也没有新增源码改动。
- 清理按服务端撤权→`connection:clear`→关闭双浏览器→停止beta完成；活动槽不存在，53952/7802/51999/55148/16608均无监听，专用任务回到idle。beta内部收尾成功与外层PTY在Ctrl+C后退出1分开记录。新生成的167字节已撤权下载未读、未删，留在本批Playwright输出目录；既有失效下载未动。该单次`silent`样本不验证公开发言，`proactive_wake_verified`不因它自动翻转。

## 2026-09-01 — B21（新手早期来源可启动，跨手后合法丢弃）

- 复用B18稳定项目MCP与既有专用游戏任务，受控beta在随机回环端口55148启动；两席在第0手分别确认公开范围，A激活一份新连接。开窗前本人投影为`hand_active=false`、A席AI `ON/IDLE`、`active=false`、冷却0，生命周期文件只有header、0个评估开始/终态；没有玩家公开消息。
- 本人先开启最多1次/120秒的新窗口，再让A/B Ready，使第1手首个行动窗口成为开窗后的早期牌局来源。整批严格只有1次原生queue，无第二来源、补发或重试；页面从0/0/0进入1尝试/1接收/0结清，专用任务自动开始，最终回合28.810秒完成。
- 权威记录第1手于`1788260043331ms`开始，评估于`1788260067032ms`真正启动：相对开手23.701秒，距A行动截止约6.302秒。第2手于`1788260076709ms`开始，模型在`1788260077813ms`以`silent`提交后被权威按`hand_advanced`合法丢弃；start→terminal为10.781秒，终态晚于换手1.104秒。两页均无AI气泡符合该终态。
- 最终窗口因次数上限停止，尝试/接收/权威结清为1/1/1、`cleanup_ok=true`、`failure_code`不存在。这不是诊断字段失效：本次`ai.start`成功，所以没有失败码可报告。它与B20“任务启动时旧手已推进”的时间线形成对照，支持旧手待办失效假设，但不能把B20历史精确码从`unknown`改写成`intent_not_found`。
- 去敏文件捕获完整、0 dropped，进程收尾回执另证`write_acknowledged/close_succeeded/run_complete=true`；离线摘要因SeatAiStore记录中没有行动窗口来源行而为`partial`（缺`source`），仍确认1个start和1个`hand_advanced`终态，不能用手开始时间冒充精确source→start。页面窗口驻留70.249秒含开窗、Ready、浏览器轮询与等待，不是模型推理SLA；浏览器控制台0 error/0 warning。本批没有源码改动，也没有重跑Node、变异或脚本浏览器套件。
- 清理已按服务端撤权→`connection:clear`→关闭双浏览器→停止beta执行；活动槽不存在，55148/7802/51999/16608无监听，专用任务回到idle。beta报告`normal_close`且记录器分项全成功；外层PTY在Ctrl+C后退出1，不能据此声称beta进程退出0。新生成的167字节已撤权下载仍在本批Playwright输出目录等待真人删除，B18～B20历史下载未读未动。`proactive_wake_verified`保持false；下一步应决定是否在正常30秒行动时限内缩短通知到`ai.start`的23.701秒，或设计不依赖真人超时的牌局内公开终态样本，不再重试B20/B21窗口。

## 2026-09-01 — B20（牌局内原生通知启动失败，已补受限诊断）

- 复用B18稳定入口和既有专用游戏任务，两席Ready后第1手进入行动期；本人开启最多1次/120秒窗口。A没有点击或补提示，专用任务自动开始并在17.426秒后完成，但页面于20.342秒以`wake_start_failed`停止，尝试1/接收1/结清0，两页均无AI气泡。
- 去敏生命周期回执记录第1～4手及第3手一条B公开消息，0个评估开始、0个turn、0个合法终态。任务启动早于该B消息，故该消息不是本次queue来源；更早的具体扑克待办只能写`unknown`。本批按停止条件不重试。
- 失败暴露观察盲区：模型命令面已有精确业务码，窗口只保留通用失败。现只增加受限`error_code`/`failure_code`诊断，拒绝自由文本和details，已知码给本人可读说明；不改通知、扑克、模型、自动重试或能力声明。
- 聚焦回归120/120；两份变异规格分别22/22与12/12全杀，脚本浏览器35/35。首轮全量1331/1332只因注册表扫描看不见三元式真实出码点，展开等价分支后最终1332/1332、68593.6824ms。两套会改写共享源码的变异驱动曾被误并发一次，确认还原后已串行复跑；该载体污染不计产品结果。
- 服务端撤权、活动槽清除、双浏览器关闭、beta IPC退出0、记录器分项关闭和51999/7802端口释放均完成。B18/B19两份166字节及B20一份167字节失效下载留待真人手工删除。`proactive_wake_verified`保持false；下一批应使用新窗口读取新增受限错误码，不重试本窗口。

## 2026-09-01 — B19（稳定入口两次串行原生通知通过，限定等待区）

- beta使用绝对`codex.exe`、固定`H:/tokengold`和既有专用游戏任务启动，启动行如实为`managed_wake=available`、`proactive_wake_verified=false`。发送器安装本身0通知；页面仍要求A逐席授权及本人每窗确认。
- 专用任务先完成一次13.951秒只读就绪回合。任务读取接口没有items，不能从载体输出判工具；牌桌权威随后显示“已收到本席宿主请求”，直接证明旧任务在重启后命中新稳定连接。
- 本人开启最多2次/180秒窗口。B第一条公开来源触发queue回合28.977秒，页面达到1尝试/1接收/1结清且双页见AI回复后，才发送第二条；第二回合24.405秒，最终2/2/2并因次数上限自动停止，双页见两条同文AI气泡。A没有再次点击或给专用任务补提示。
- 116.911秒是窗口从开启到最终观察的驻留时间，包含浏览器操作与轮询，不能当两次模型推理耗时或实时SLA。样本停在第0手、无人Ready；因此只关闭稳定入口等待区连续通知切片，不翻`proactive_wake_verified`，牌局内来源与分段时延继续开放。
- 最终页面撤权、`connection:clear`、两浏览器关闭、beta停止、7802无监听。没有第三条或重试；专用任务回到idle。B18/B19各一份166字节已撤权下载仍因工具策略边界留待真人手工删除；未暂存、提交、部署或触碰B12/B14历史资源。

## 2026-09-01 — B18（重启后原生接入、热换与双页 AI 气泡通过）

- `H:/tokengold`受管项目块已写入，真人重启后本任务工具面只出现`tokengame_table`五条既定命令；未激活时第一次`view.projection`按合同返回`model_connection_unavailable`，证明服务器已加载而权限默认关闭。
- 新建本地两人房并下载A席连接：激活后原生投影成功；页面撤销后同一活动槽明确返回`model_command_token_rejected`；同席重新签发并覆盖稳定槽后，不重启Codex或MCP即恢复成功，闭合B17缺失的运行中换发事实。
- B独立浏览器公开发言后，当前Codex会话实际完成一次`ai.take_intents → ai.start → ai.resolve`，权威返回`SEAT_AI/TABLE_PUBLIC`，A与B两页都显示同文AI气泡。该链为显式“继续”触发，0 queue，不是持续主动唤醒、第二真实AI席或牌局时延样本。
- 初次浏览器观察到的唯一控制台错误是缺省`/favicon.ico` 404；页面改为空data favicon并补源码回归，定向11/11，干净beta实机复验0 error/0 warning，最终全量1329/1329、68569.4652ms。
- 收尾已完成服务端撤权、本地`connection:clear`、撤权后`model_connection_unavailable`、两浏览器关闭及7802释放。工具策略在执行前拒绝包含删除的组合命令；未改用其他工具绕过，本轮166字节失效下载仍在`.playwright-cli/`等待真人手工删除。未暂存、提交、部署或触碰B12/B14历史阻塞资源。

## 2026-09-01 — B18（稳定项目 MCP 与逐席连接热切换，本地已闭合）

- B17反复改临时MCP定义会依赖宿主重新发现服务器。B18改为一次加载的项目launcher，固定读取`.tokengame-private/active-model-connection.json`；同一MCP模块在缺文件→激活→换发→清除间逐请求重读，不需重建进程。下载源由真人保留/删除，页面撤权与本地`connection:clear`分开。
- 新增显式`codex:configure -- <项目根绝对路径>`，只管理目标项目`.codex/config.toml`中的标记块，保留既有TOML、拒绝仓库外项目/同名冲突/符号链接与超限文件、原子发布失败保留原配置。当前Codex保存项目为上层`H:/tokengold`，仓库内配置只是可移植模板；本轮未执行父项目配置或宿主重启。
- 全量首轮1327/1328暴露Codex专有配置渗入通用`src/`，已把配置器搬到`plugins/tokengame/codex/`并把beta改回宿主中立说明；定向79/79后最终全量1328/1328、66843.1965ms。
- 首轮总门禁为632/638、2存活/4未评估：两条刷新成功围栏变异被catch分支同名字符串遮蔽，四条旧规格锚点分别漂到当前beta和错误注册表。收窄断言窗口并更新同语义锚点后，三份定向规格14/14、15/15、10/10；最终总门禁638/638、0存活/未评估，`GATE=PASS`。B18自身项目/连接两组为12/12与10/10。
- 连接浏览器51/51、16786ms，覆盖两席下载/stdio/本席上下文/气泡/撤权/窄屏/无泄漏；Skill以显式UTF-8模式验证通过。第一次Skill校验因Windows Python按GBK读UTF-8而崩溃，属于校验载体编码，不记为Skill失败。
- 本批0真实模型输入、0queue、0父项目配置、0宿主重启。裁决见`REVIEW-LOG.md#b18-stable-project-mcp-activation`；下一步需用户明确允许在`H:/tokengold`写受管项目块并重启一次Codex，再以有界原生连续批次判断B17载体问题是否真正解除。

## 2026-09-01 — B17（真实连续窗口被既有任务 MCP 重激活边界阻塞）

- 独立checker先发现并修复一项真实竞态：queue ACK返回前若权威已resolve，随后撤权/OFF清空surface，旧实现会丢失已结清回执并把窗口永久判pending。新增专门回归，主线程42/42、19/19及1条实际变异通过；最终全量Node1315/1315、69343.9369ms。
- 三个彼此清理后的有界批次共4次原任务输入、1次queue。attempt 1 readiness在20.257秒内实际调用一次`view.projection`；第一来源已触发queue，但B17外壳在最后调用后90秒空闲退出，25.053秒queue回合没有游戏MCP调用，UI以1尝试/1接收/0结清和`wake_result_unknown`停止，未发送第二、第三来源。
- 外壳改为360秒空闲、480秒硬上限，并在撤销临时连接文件后自动退出；独立生命周期脚本实测`connection_file_removed`正常退出。attempt 2使用新服务名、attempt 3恢复已成功过的旧服务名，两次readiness任务分别12.163秒、7.859秒完成，但均没有启动MCP进程或产生工具调用，因而都在queue前停止。
- B17全程0次`ai.start`、0次`ai.resolve`、0条AI公开，连续宿主产品路径仍未验证；不能把载体失败算作扑克/协调器失败，也不能用B14三次单次公开补成连续结果。三批逐席权限、临时配置、私有连接文件、浏览器、beta、端口和本批进程均已清理，B12/B14历史阻塞资源未读未动。
- 当前裁决见`REVIEW-LOG.md#b17-native-managed-wake-carrier-boundary`。下一步先形成稳定的项目级MCP激活/连接切换机制，再开新的真实连续批次；不再用同一临时进程反复消耗原任务输入，不全局reload或重启宿主。

## 2026-08-31～09-01 — B16（本人启停UI本地已验证，原生连续仍未跑）

- 在原牌桌接B15 API：每窗确认、次数/时长实际上限、显式启停、同键核对与分项回执。复用现有轮询，只显示本席窗口，不暴露目标配置，不增加模型或协调器。
- 主线程看截图发现新请求未知时附带旧窗口2/2及上限原因，已修展示归属并加回归/变异。独立checker实证并修复可选模块挂起阻断会话恢复、迟到模块丢失未完成授权屏障两项P2；新增8项bootstrap测试含多操作交叠和跨会话迟到回调，保留两批先红后绿。
- 最终全量1314/1314、69844.1736ms，最终补充Playwright35/35、9360.2016ms，纯状态模块正式10条变异全部被杀且复制/还原SHA相同。主线程先Browser真实双页，再做脚本窄屏/故障注入，最后刷新当前源码复验；脚本不是原生模型，数字不与旧轮次相加。
- 最终30输入摘要`ff5b7b21bfad2f0949de7eb9cb7de1d8b44078c9a294a2506e0906bb1e7820f4`；原始证据在仓库外`H:/tokengold/.codex/b16-wake-controls-20260831-7c82d6a1/`。首轮路由夹具崩溃、刷新后忘展开details、错误文案断言、spec输出被误按TAP解析、早期变异1ABORT均保留并分项解释，不改写为一次全绿。
- 当前事实与恢复点为`REVIEW-LOG.md#b16-managed-wake-controls`、`RETURN-HANDOFF.md#b16-当前工作`。真实模型/原生queue/原生任务输入均0；历史清理策略阻塞、连续原生、第二真实席、Claude、异地和四真人45分钟门仍开放，未暂存/提交/归档/部署。

## 2026-08-31 — B15（有界通知API本地验证完成，尚无启停UI和连续原生样本）

- 在原`TableWebHost`内连接逐席有界通知，不另建协调器或模型；默认关闭，配置固定原生任务后仍需本人单独开启最多4次/10分钟窗口。queue ACK不释放单槽，必须观察精确intent→turn→resolve；OFF、撤权、离桌和关停保留原有围栏。
- 独立检查发现并修复同席跨绑定旧未决/清理保护遗漏、发送前到期误记为已调用两项P2；实际新反例先3失败/1通过，修后六组100/100。主线程随后发现发送器5条变异中1存活，补齐race已完成而await未恢复的取消回归；生产发送器原有检查是必要的，未删除或放宽它。
- 最终全量1239/1239、69362.1688ms，无失败/跳过/取消；发送器5、B10共享探针8、窗口18，共31条实际变异全部杀掉，0存活/未评估，源码前后身份匹配。首轮失败、此前1234/1238全量及夹具修正记录分列，不相加为独立覆盖。
- 本地整合确实经过两真人HTTP、两逐席MCP进程与两个脚本queue接收进程，先公开再沉默，验证来源关联、单槽、底牌隔离和撤销；不算真人UAT、浏览器或原生模型。真实模型、原生任务输入及真实queue均0。
- 唯一当批记录为`REVIEW-LOG.md#b15-managed-wake-session`；接口说明为`docs/MANAGED-WAKE-SESSION.md`。最终29文件摘要`f3384f12d370fb6d181c0b1637968ce2c622c8bf0176e7c654d7ec825c248aa0`，检查后仅1个测试文件改变。B14 Gate9策略阻塞未重试，未暂存/提交/归档/部署。
- 当前继续点是本人显式启停UI和有界真实连续验证，不重新设计一次性探针，不调整模型或扑克时钟。主动产品父节点仍开放，Claude、第二真实席、异地和四真人45分钟仍未验证。

## 2026-08-31 — B14授权后（真实单席自动公开已证，延迟标签已修，清理仍部分阻塞）

- DEC-20260831-003确认具体席位权限后，复用原任务，1次实际MCP准备、3个不同公开来源各一次queue，共4次原生输入、7次原任务游戏MCP调用、3次真实评估与3次成功公开；无A再次点击或新提示。未用完12/4上限，无新goal或常驻模型循环。
- 两例等待区、一例第1手牌内；源事件到公开43.857/46.785/43.660秒。模型上下文记录gpt-5.6-sol/max，未覆盖；不是供应商证明、三手真人测试或实时SLA。第1例只采到历史，第2/3例看到双页同文座位气泡。
- 第3例实际跨街，但旧UI漏了权威迟到标记。只修`table-view-model`的字段映射；旧人工夹具14/14不能捕获问题，改为真实producer回归后20项中5项先红，修复后20/20、相邻45/45；恢复旧映射的1条变异被杀。隔离上下文复核未新增finding，未再跑测试冒充新覆盖。
- 主线程另开本地脚本UI夹具，14项检查通过，包括双端两种显示区的迟到标记、隐藏/恢复、同街不误标、文字隔离、静默额度、TTL及诊断状态。两页error/warn查询为空、两张截图已查看；窄屏not_run。夹具首轮缺DueWork导致未开手，修正后run-02验证；两轮分别保留，不归为产品失败。修复后没有追加模型/MCP/queue。
- 实机权限已撤销，旧连接明确被拒；临时配置移除、beta及控制器正常退出，18条事件、20条捕获记录和关闭回执对应。失效私有文件及宿主管理MCP的清理被工具策略拒绝，未绕过，Gate9仍blocked。脚本UI服务另已退出0、端口释放。
- 唯一当前裁决见`REVIEW-LOG.md#b14-native-public-replies`；最小事实包在`evidence/probes/b14-codex-queue-native-20260831/`，原始日志与修复证据在两个独立artifact目录。Codex Gate5只在固定版本单席探针中pass，Claude仍not_run；不关闭父节点、不翻转默认能力、不宣称完整朋友内测可用。既有脏文件保留，未暂存/提交/归档/部署。

## 2026-08-31 — B14（持续测试获授权，具体AI权限待确认）

- 用户“允许长期测试”已记为DEC-20260831-002，使用有界批次，不逐条申请普通模型输入，不创建定时任务或goal。
- 本批冻结370文件与80项既有脏路径、暂存空，B13七份源码哈希一致；只读原任务状态notLoaded，未将其当成工具就绪。
- 独立回环牌桌A已落座，B未确认，未开手。AI读取本席牌面、公开发言和私有凭据下载在执行前触发具体权限确认；已向用户询问，未绕过。本批0原任务输入、0queue、0游戏MCP调用、0真实AI评估；未重跑Node测试或变异。
- 两页关闭，beta与控制器IPC正常退出0、非强制、输出完整；footer与关闭回执同run_ref，0个采纳AI事件，端口无监听。只删除本批空目录；没有新连接文件或MCP配置，B12失效文件未动。
- 服务驻留583.677秒包含建房、工具限制处理与等待，不是模型推理或产品性能。选择器不匹配、CDP下载方法不支持、权限确认拦截分别记录，不直接归因产品或Dual。
- 无产品源码变更，未暂存、提交或部署；当前事实见 `REVIEW-LOG.md#b14-native-readiness-permission-boundary`。具体权限确认后先测原任务MCP，Codex Gate5仍blocked、Claude仍not_run，不能用本批收尾证明完整主动AI。

## 2026-08-31 — B13（本地关停完成，原任务 MCP 仍未验证）

- 用户“继续”仅恢复同一已确认路线的本地开发；开发前77个既有脏文件、367份文件身份及空暂存已冻结。没有新增原生游戏输入、queue或真实模型调用，未恢复已关闭窗口。
- 只读核对当前安装代码和CLI生成schema，区分目录配置、独立MCP与原任务运行时就绪。协议有threadId/runtimeStatus不等于原任务实际可用；全局reload无单任务参数，未调用。B12工具发现失败根因仍unknown。
- 补受控Node fork/IPC关停，复用同一close流程，不新增HTTP控制接口；输出流、捕获文件、I/O回执和进程退出分别判定。正式检查修复关停中父断连、首因掩盖截断与晚到输出错误三个假成功边界。
- 最终46/46定向及相邻测试、Node 10460.5787ms；主线程最终脚本AI非空捕获整合26/26、197.6686ms；隔离副本9条变异全部杀掉，0存活/未评估，370文件已还原。591条锚点只静态核对，未跑全量、UI、13手或真人验收。
- 11条显式node:test命令、两次诊断尝试、两次脚本整合及三条变异驱动分别记账；检查前38项、早期26项、故意红测及夹具事件名错误均保留，不合计为独立通过数。实现分工两次载体中断单列，原因unknown。
- 源码7文件、证据49文件清单及完整台账在 `artifacts/b13-host-readiness-shutdown-20260831/`；B9–B12冻结证据163文件核对未变。旧beta16608未动，B12失效私有文件不绕过策略删除，时点进程快照不冒充清理完整。
- 本批唯一本地裁决见 `REVIEW-LOG.md#b13-host-readiness-shutdown`，当前恢复点见 `RETURN-HANDOFF.md`。按Trellis补真实异常路径测试及操作规范，但不关闭主动AI父节点；Codex Gate5 blocked、Claude not_run。manual_closeout下没有暂存、提交、部署或归档。

## 2026-08-31 — B12（接入受阻，已停止新增原生输入）

- 用户“同意验证”对应DEC-20260831-001：复用原游戏任务，最多3次输入含1次queue，结束撤销；不改模型、规则或部署状态。
- 启动前B11的12份源码均与最终清单匹配，工作树77个既有改动、暂存区空；本轮不重跑整套回归。
- 宿主只读接口现在能返回B10工具调用条目，另存本轮补充记录；原始B10证据保持不变，工具参数/完成状态不替代缺失的权威返回正文。
- 两次原任务输入共23.364秒，均在工具发现阶段停止，未调用游戏工具；没有Ready、来源消息、queue或真实AI评估。独立MCP以同一文件读取成功，仅证明本地传输正常。
- 进一步只读原任务的可见工具输出，确认B10实际返回hand_advanced：回答跨手被丢弃，不是silent；新摘录单独保存，不改B10冻结产物。
- 真人撤销后旧权限HTTP403；临时配置已删除，浏览器、beta及端口已停，旧beta16608未动。私有文件删除被工具策略阻止，匹配MCP进程仍可见；Ctrl+C后缺捕获尾行与关闭回执。清理仅partial，不能写全部完成。
- 下一步先解决同一任务MCP发现/刷新和可核对的关闭方式；本窗口余量不结转，不继续烧模型重试。唯一事实记录见REVIEW-LOG.md#b12-native-receipts-window。

## 2026-08-31 — B11（本地终态取证完成，真实闭环未重测）

- 用户“继续”后选择已确认路线内的本地可观察性修补；方案与 R1–R8 验收矩阵见任务 research/b11-ai-lifecycle-receipts-20260831.md。
- 复用权威事件，增加默认关闭、去敏、有界的本地记录与离线时序汇总。不改 UI 或规则，不追加真实模型调用，不追认 B10 未知终态。
- 文件内容完整性、写入回执、资源关闭分别报告；离线读取成功不能覆盖运行写入/关闭失败。独立复核修复畸形链、身份冲突、时差误报与错误码扫描，并同步失效变异模板。
- 最终全量1110/1110（显式排除默认端口1项），默认端口另跑7/7；本批20项与相邻17项变异全部杀掉，0存活/未评估；两席连接浏览器51/51，5张最终截图已查看。未重跑四人13手或真人验收。
- 最初全量1094通过/1失败、检查者31通过/16失败及其他初版反例均保留。全部次数、耗时、来源与边界见 `REVIEW-LOG.md#b11-ai-lifecycle-receipts`；使用说明见 `docs/AI-LIFECYCLE-RECEIPTS.md`。
- 开发前69个既有脏文件及未提交改动保留；无关运行代码和B9/B10冻结证据未改，旧beta16608未动；没有暂存、提交、部署、真实模型或queue调用。
- 下一步须重新取得一次有限真实宿主窗口授权，使用新回执验证同一来源→一次真实评估→唯一合法终态。Codex Gate5仍blocked，Claude仍not_run；不追认B10终态，不承诺时延改善。

## 2026-08-30/31 — B10（一次真实queue验证，完整门禁未通过）

- 用户“同意验证”对应DEC-20260830-003；只复用原任务，3次任务输入含1次真实queue，无新任务或模型设置覆盖。
- B公开消息后A无新点击/提示，原任务自动启动并完成49.009秒回合；观察到一次THINKING，跨入第2手后回到IDLE，AI公开发言为0。
- 读取接口对3个回合均返回items为空；合法沉默/丢弃终态及精确调用数unknown，不能将IDLE或queue退出0写成通过。Codex Gate5为blocked，Claude仍not_run。
- 真人撤销后，单独的只读HTTP旧令牌检查返回403/model_command_token_rejected；本轮连接文件、临时配置、服务、MCP和浏览器均清理，旧beta16608未动。
- 产品48文件、B10探针3文件、B9的25份证据及B10本地23份证据均核对未变；本轮未重跑产品测试、未提交部署。唯一记录见REVIEW-LOG.md#b10-native-queue-wake-probe。
- 下一步只做同路线本地终态取证与时延诊断，不自动追加实机调用，不改扑克时钟或把被动问答当成主动AI的等价交付。

## 2026-08-30 — B10（同任务queue候选的本地准备）

- 默认关闭的单次探针、测试和变异规格已实现并独立复核，见 `DEC-20260830-002` 与 `REVIEW-LOG.md#b10-queue-wake-probe-preparation`。
- 修复两处边界缺陷并补平台分支可达性，最终117项定向测试、89项相邻回归与8条变异全部通过；117项在52文件哈希核验副本中再次通过。初版失败和红测没有抹去。
- B9日志复算：玩家来源事件到权威公开33.460秒，公开后收尾11.556秒；超过该次行动截止3.822秒。没有新增模型性能样本。
- 48个B8/B9运行文件、25份B9证据未变；临时测试副本已清理，旧beta未动。真实queue/原生任务输入/模型调用均0，双方Gate5仍not_run。
- 已询问下一实机窗口的有限授权，未收到答复；暂不恢复连接、不发送任务输入、不提交或部署。不把本地准备完成写成主动唤醒完成。

## 2026-08-30 — B9（当前Codex单席真实接入探针）

- 用户明确授权见 `DEC-20260830-001`。一个专用游戏任务、五轮输入、九次游戏MCP；当前Desktop26.825.6671.0，任务元数据gpt-5.6-sol/max，未覆盖模型设置或另配API。
- 原生 `ai.start` 得到本席牌面和公共聊天；两次真实生成，一次跨手丢弃，一次sequence16公开并被两页看见。成功轮次44.917秒，不能称为实时性能通过。
- 真人撤销后，同一任务在连接文件仍存在时被拒；随后真人仍能操作。临时配置/私有文件/MCP/测试服务和浏览器均已清理，旧beta16608未动。
- 发现首次建房长邀请码在390px溢出，原35项窄屏检查在reload后漏测。按既有UI规范小修CSS，增加16项复制前后回归，51/51一次通过（6321ms）；定向Node20/20（1364.056ms），七合同校验匹配。原失败记录和内置Browser检查器取值错误单列保留。
- 唯一闭环 `REVIEW-LOG.md#b9-real-host-seat-probe`，恢复点移到主动唤醒与时延探针设计。两宿主Gate5仍not_run；完整入口、第二真实席、Claude、异地联机及真人UAT未完成。
- B8全量925测试/557变异/209项13手未在本轮重跑；没有提交、部署或归档父任务。

## 2026-08-30 — Codex 接手 / B8（本地链路完成）

- 接手基线 `bbdcf2b`，B6-1/B6-2/B7 已提交，开始时工作树干净。本轮实跑基线 `npm test`：875/875，47,038.9874 ms；不是照抄旧记录。
- 当前实现边界冻结在 `TAKEOVER-PLAN.md`：逐席 AI 授权/撤销、权威 `ai.start.model_context`、浏览器下载与 MCP 私有文件读取。不新增大厅/匹配/托管下注，不改宿主全局配置或放开远程监听。
- MCP连接文件、逐席权限、权威上下文和界面已完成；实现与检查分工保留单一协调器及托管。新上下文审查推动修复5项P2，具体原因和回归见 `REVIEW-LOG.md#b8-seat-model-binding`。
- 页面保留现有语义 HTML 控件，不为游戏测试重写成 Canvas；新增只读 `render_game_to_text`。`advanceTime` 只刷新投影，不能快进权威时钟；多人规则时钟另由既有核心测试验收。
- 最终全量925/925（60206.7141 ms），40规格557变异全部杀掉、0存活/未评估，GATE=PASS（整轮597553 ms）。首轮925测试通过但变异1存活/1未评估的失败记录保留；补真实Host到达断言和屏障finally释放后完整复跑。
- 两Chromium＋两MCP stdio的35项连接验收通过（7352 ms）；四Chromium的209项牌局验收到第13手通过（151360 ms）。四人首轮旧隐私文案断言失败也保留。截图已实际查看，最终15个产品文件与浏览器验证副本一致；两类浏览器均非真实模型。
- 下一步：先明确真实宿主配置和模型调用授权，再验证当前Codex单席真实生成到气泡的回路，另测是否需要用户点击。Claude当前安装状态unknown；双方无点击唤醒、异地联机与四真人45分钟UAT仍未验证。
- 本批唯一完成裁决在Review Log，接续见 `RETURN-HANDOFF.md`。保留manual_closeout：未暂存、未提交、未推送，不归档父任务。

## 2026-08-25

- 已按 `plugin-creator` 生成仓库内 `plugins/tokengame` 骨架；尚未安装到全局，也未发布。
- 已确认本轮边界：只验证 Codex Hook/MCP、本地桥、伪权威事件服务与最小 Web 观察页，不实现完整德州扑克。
- 已实现两个隔离的回环服务：`fake-authority` 管理服务端时钟、事件序号、一次请求额度与幂等；`bridge` 作为 Hook/MCP 的唯一入口。
- 已补齐仓库内插件的 `UserPromptSubmit`、`Stop`、`PreToolUse` Hooks、显式回答补交 MCP 工具与 `$tokengame` Skill；插件结构校验通过。
- 普通提示在 Hook 本地解析后直接结束，公开提示在权威服务接受前失败关闭；自动化已证明这两个不变量。
- 已实现 Canvas 牌桌事件观察页和权威事件侧栏；公开 Prompt、Model、Answer 三阶段只反映真实事件，不模拟扑克规则。
- `npm test` 9/9 通过；插件结构校验通过。
- Playwright 已点击关闭/重开窗口并完成公开 prompt→answer 流程；全页控制台错误为 0，最终截图为 `artifacts/full-page-smoke.png`。
- 浏览器发现的到期窗口状态分叉已修复并增加回归测试。
- 已写入协议、安全边界、宿主探针清单和验收证据。
- 当前结论：本地协议探针通过；真实 Codex Desktop 宿主仍待授权安装验证，路线不进入完整牌桌开发。

## 2026-08-26 — TG-L3 多人牌桌垂直切片

- 用户已最终确认 PRD，任务从 `planning` 切换为 `in_progress`。
- 固定范围：A/B/C/D 四个独立测试身份、服务端唯一权威状态、逐玩家隐藏信息投影、一手完整无限注德州扑克、标准摊牌与至少一个三人边池场景。
- 规则策略：成熟无限注德州扑克优先；TokenGame 只定义 Codex 公开 AI、隐私与本地测试边界。超时默认可过牌则自动过牌，否则自动弃牌。
- 实现路线：先新增独立扑克领域模块和表级权威包装，再接 HTTP/SSE 与浏览器 UI；保留现有 Codex 桥接探针合同与 11 项回归。
- 验证要求：Node 确定性规则测试、HTTP 身份隔离测试、现有 `npm test`、Playwright 多身份操作与截图/控制台检查。
- 当前 TODO：实现领域状态机、身份令牌投影、API/SSE、四人 UI、规则符合性测试和浏览器验收。
- 已新增 `src/game/holdem.cjs`：标准发牌/位置、四轮下注、最小完整加注、短额 all-in、主池/多层边池、七选五牌型、平池奇数筹码、摊牌和弃牌获胜。
- 已新增 `src/authority/table-store.cjs`：四身份令牌、逐玩家投影、版本校验、幂等动作、超时自动动作、事件序列和自愿亮牌。
- 已把个性化牌桌状态、动作、亮牌、重置和 SSE 路由接入权威 HTTP 服务；现有 11 项 Codex 桥接回归继续通过。
- 新增 `test/holdem-engine.test.cjs`，并扩展 HTTP/SSE 集成测试；完整 `npm test` 当前 23/23 通过，原有 11 项 Codex 桥接回归保持通过。
- 已将观察页替换为可操作的四人 Canvas 牌桌；按钮完全由服务端合法动作投影驱动，支持下注金额、all-in、自愿亮牌和 A 发起下一手。
- 四个隔离 Chromium context 已分别以 A/B/C/D 身份经 UI 完成 checkdown 摊牌、四人 all-in、加注后三人弃牌及赢家自愿亮牌；公共状态一致、隐藏底牌隔离、控制台错误为 0。
- 浏览器验收中发现并修复公共牌参数错误、牌桌纵向拉伸和未发公共牌误显示牌背；补充重置幂等重放与平池奇数筹码回归。
- 当前下一步：执行 Trellis 独立质量检查，修复发现项，更新状态/验收证据并收口本地任务；不扩大到生产认证、数据库或 Codex 内嵌 UI。
- 用户体验验收反馈：牌局机制可以，但 AI 助手应位于各玩家座位旁，玩家与 AI 的公开谈话应以聊天气泡呈现；因此用户验收保持开放。
- 解释边界：只显示服务端已接纳的 `AI_PROMPT_PUBLISHED` / `AI_ANSWER_PUBLISHED`，不显示普通 Codex 会话；按 `actor` 与 `request_id` 派生每席最近一组，完整历史保留在右侧事件流。
- 实现计划：在 Canvas 上方增加可访问的四席 AI DOM 覆盖层，显示玩家 prompt、AI 生成中与 answer；扩展 `render_game_to_text()` 和四浏览器 smoke，验证四视图一致、长文本不挡牌、未知来源不显示、控制台零错误。
- 已实现 A/B/C/D 四席 AI 同伴与逐席最近公开对话；合法 prompt/answer 按 `actor + request_id` 配对，等待回答显示生成中，普通/未知/孤立/错席/重复/迟到事件均不能污染当前气泡，HTML 标记只按文本展示。
- 独立 Trellis 检查发现并修复两项一致性问题：非法 answer 曾误点亮全局 Model/Answer；公开事件计数超过 80 时 DOM 曾截断历史。现全局阶段只跟随最新合法会话，四视图事件计数与实际列表均为 87/87。
- 最终主验证：`npm test` 23/23，四窗口 Playwright 公开 AI、16 动作 checkdown、四人 all-in、加注弃牌与自愿亮牌全部通过，`console_errors=[]`；桌面和 560px 窄屏截图已人工检查，Canvas 技能客户端回归通过，服务端口已释放。
- 当前下一步：请用户重新体验座位旁 AI 与聊天气泡。当前桥仍只端到端产生 A / `ai:a`；B/C/D 前端通用投影已验证，但不等于四个真实 Codex 会话已经绑定。

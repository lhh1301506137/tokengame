# Codex 当前恢复点：B33候选发布与两好友真人验收（2026-09-05）

第一次相对化迁移与重启已由`DEC-20260901-001`执行；B25随后证明当前Desktop不能可靠从相对`cwd`加载牌桌工具。`DEC-20260901-002`授权的绝对`cwd`恢复、用户第二次手动重启和重启后直接工具核对现已全部执行。长期同范围测试仍依据`DEC-20260831-002`，具体席位权限依据`DEC-20260831-003`。本次B33提交、非强制推送和CI核对另由`DEC-20260905-002`一次性授权；没有后台模型循环，不含部署、额外重启、全局刷新、改模型或新任务。

状态：远端B32候选及四作业CI已通过，但真人尚未开测。B33先对标成熟德扑实现，补齐规则反例与破产席手间固定补测试筹码；双浏览器产品闭环及最终串行完整门禁均已通过。实现已提交为`610a8a175ec7938a894227bd7853f6259ab91659`，推送已获授权但远端与CI仍待实际回执。事实见[B33记录](./REVIEW-LOG.md#b33-poker-maturity-and-refill)。

## B33 当前工作

- 不整仓替换引擎；PokerKit、`@hivetech/poker-engine`与公开规则只作为行为oracle，具体覆盖和后置范围见`docs/POKER-MVP-COVERAGE.md`。
- 当前产品新增的唯一规则决策是`DEC-20260905-001`：零筹码席手后SIT_OUT，只在手间由真人固定补至200，补完仍需单独Ready；无自定义金额、自动补、账户或模型权限。
- 双隔离Chromium破产闭环11/11、控制台0错误；首轮Node注册1488、通过1487、平台条件跳过1、失败0。首轮完整gate为exit1（712杀掉、3存活、2未评估），定向修补后的中间gate为1490/1490与717/717；收尾再修破产恢复死状态后，最终字节串行gate为Node1491/1491、变异718/718、exit0。
- 真人两设备、两个独立Codex、真实HTTPS、双方同手AI气泡和十手签字保持`not_run`。本批不启动公网或原生模型。

## B28 当前工作

- `DEC-20260901-002`已完整执行；不再迁移父项目配置或要求重启。B25相对`cwd`失败是已修历史，B27绝对`cwd`工具恢复是直接运行时事实。
- managed横幅、固定目标页面和项目Skill现在都要求：命令启动beta后结束当前回复，把该游戏任务交还为空闲；不要在同一回合看守页面、等待对手或继续开发。queue“已接收”不等于模型开始或权威结清。
- 每窗勾选新增“目标任务已结束当前回复并可接收新回合”的本人声明；这只是操作确认，系统尚不能从宿主遥测证明任务确实空闲。
- 聚焦首轮18/19后统一文档措辞，最终19/19；浏览器46/46、0错误且6项清理通过；完整Node1356/1356。Skill普通校验的GBK载体失败用同一校验器UTF-8模式复核通过。没有跑变异门禁。
- 唯一下一步：真实本地朋友组合使用已经结束启动回复、保持空闲的各自游戏任务，继续以权威start与唯一terminal验收。当前不自动创建新任务、不进入远程或第二模型；`proactive_wake_verified=false`且`PLAYABILITY_GATE`仍blocked。

## B20 历史工作

- 真实第1手行动期内开启最多1次/120秒窗口；原生任务17.426秒完成，页面20.342秒停止。任务完成只证明宿主载体被唤醒，不证明`ai.start`或权威评估成功。
- 去敏回执记录第1～4手与第3手一条B公开消息，但没有评估开始、turn或终态；B消息晚于任务启动，不能把它写成因果来源。没有补发、第二来源或重试。
- `ModelCommandSurface`现在只把符合固定语法的业务错误码放入失败观察账，`ModelWakeSession`投影可选`failure_code`，本人页面对已知码给受限说明；自由文本、details和上游正文继续失败关闭。通用窗口原因不变，能力声明不变。
- 首轮全量1331/1332只暴露静态注册表扫描看不见三元式真实出码点；展开等价分支后最终1332/1332、68593.6824ms。相关变异22/22与12/12串行全杀，脚本浏览器35/35。变异驱动会改写共享源码，后续不得并发运行。
- 下一批不是重试本窗口，而是新授权/新窗口复现一个有界牌局来源，读取新增稳定码后决定修业务拒绝还是调整测试时序；任何一次成功仍需权威start与唯一终态共同证明。

## B19 历史工作

- beta发送器固定真实绝对`codex.exe`、`H:/tokengold`和任务`TokenGame 临时单席接入验证`；启动本身0通知，页面仍要求逐席授权与每窗确认。
- 旧任务的13.951秒只读就绪回合在宿主接口中无items，但牌桌权威转为“已收到本席宿主请求”，证明它在重启后命中`tokengame_project`，没有继续依赖B17临时服务。
- 两次queue均观察到专用任务完成、权威结清和双页AI气泡；第二次严格晚于第一次结清。0第三条、0重试；窗口按2次上限自动停止。
- B19当时的下一步是牌局进行中来源；B20现已执行该样本但在`ai.start`失败，当前继续点以上一节为准。未来若分段仍明显超过玩家操作时钟，应把“垃圾话异步显示”与“行动建议实时到达”拆成不同产品承诺。

## B18 历史工作

- `src/run-project-mcp.cjs`固定项目私有槽位并启动既有MCP；`src/shared/model-connection-file.cjs`统一精确schema、16KiB、回环origin、令牌和非符号链接校验。MCP每次调用重读文件，同进程可在激活、换发、清除间切换。
- 真人CLI：`npm run connection:activate -- <下载绝对路径>`原子发布完整文件，失败保留旧连接且不删除下载源；`npm run connection:clear`幂等且只删固定槽位。页面服务端撤权和本地清文件不是同一动作。
- Codex配置器在`plugins/tokengame/codex/`，只改显式项目的受管标记块；仓库必须位于该项目内。首次新增服务器按宿主合同需要一次重启，以后换席/换桌不改MCP定义、不重启。
- 首轮全量1327/1328因宿主专有配置渗入`src/`而失败，已通过架构搬家修正；首轮门禁632/638暴露2存活/4未评估，已补精确围栏断言和当前锚点。最终Node1328/1328（66843.1965ms）、638/638变异、Browser51/51（16786ms）、Skill valid。
- 不再使用B17临时readiness模式，也不再重复父项目配置或宿主重启。下一步复用已加载的稳定`tokengame_project`，按既有有界窗口合同做一次持续通知批次；保持默认关闭、固定本任务、实际resolve释放单槽，并把显式模型回合与queue触发分开计数。

## B17 历史工作

- attempt 1：readiness成功；第一来源queue已接收，但专用MCP外壳在最后请求90秒后退出，原任务随后没有工具调用。UI直接观察到1尝试/1接收/0结清并以`wake_result_unknown`停止；按冻结规则没有发第二来源。
- attempt 2/3：先把外壳延长到360秒空闲/480秒硬上限并加入连接文件撤销退出，独立脚本实测可清理；先换新服务名、再恢复旧服务名，两次readiness任务都完成但MCP进程从未启动。事实只证明既有任务未重激活该项目工具面，精确宿主缓存/生命周期规则unknown。
- 三批合计4次原生输入、1次queue、1次实际游戏MCP调用、0次start/resolve/AI公开。所有B17权限、配置、连接文件、页面、beta、端口和本批进程已清理；没有后台窗口。B12/B14历史策略阻塞未重试。
- checker修复`model-wake-session`在resolve先于ACK、撤权先于sender返回时丢回执的竞态；42/42定向、19/19控制、1条实际变异和全量1315/1315（69343.9369ms）通过。没有重跑浏览器35项或全仓变异。
- 下一实现方向不是再发同类readiness，而是让项目MCP在游戏前稳定加载、席位连接在不重启MCP的情况下换代；在代码和本地测试闭合前不再开真实连续批次。

## B16 历史工作

- 原牌桌“连接我的会话 AI”内已有“自动通知（本地实验）”：本人填固定任务UUID，选次数/时长并每窗确认后开启，可停止或同键核对。默认关闭，沿用原协调器及权威规则；没有第二个模型、调度器或后台任务。
- 实际上限和本席窗口来自既有 `/api/view`，不泄露目标配置或对手窗口。接收与resolve分开，后者含公开/silent/丢弃，原生回合状态仍unknown。停止不能撤回已接收消息；AI OFF/撤权继续禁止迟到公开。
- 已修复新请求混入旧窗口回执；可选模块与会话恢复解耦；模块加载前的OFF/换绑/撤销在途屏障由本标签页当前会话保留，多项交叠和旧session回调均有回归。35项最终浏览器验收直接验证了模块挂起仍恢复/聊天、迟到加载与未决OFF/后发撤销交叠；320/390px先断言6个可见控件再验布局。
- 最终Node1314/1314、69844.1736ms，浏览器35/35、9360.2016ms，纯状态模块10条实际变异均被杀。1306、1310与29/30/33项浏览器分别属于之前代码，不能外推为最后两项修补已受检。项目无lint/typecheck脚本，独立上下文不等于已核验的跨模型外审。
- 当前30项代码/测试输入摘要为`ff5b7b21bfad2f0949de7eb9cb7de1d8b44078c9a294a2506e0906bb1e7820f4`，记录在仓库外 `H:/tokengold/.codex/b16-wake-controls-20260831-7c82d6a1/verification-03/source-manifest.json`。早期来源、夹具和封装器失败保留。操作说明见`docs/MANAGED-WAKE-SESSION.md`。
- 这一批只完成本地控件及安全边界，不证明真实连续唤醒、异地朋友可用、Claude或第二真实AI。B12/B14策略阻塞不重试；手工收口，不暂存/提交/归档/部署。

## B15 历史工作

- 连接生命周期由原`TableWebHost`管理；beta明确配置固定游戏任务的发送器后，还要本人另行开启最多4次/10分钟的窗口。开发接口见`docs/MANAGED-WAKE-SESSION.md`，牌桌尚无启停按钮。
- queue ACK不释放槽；同intent→turn的实际resolve才允许下一次通知。停止后的未决请求不能靠新键或新thread绕过；一项原生任务在同服务内永久配对同一席，不能转借给对手。
- 发送器复用旧B10的无shell/有界进程边界，HTTP取消会传到响应体读取；OFF/离桌/撤权/关停停止后续发送。不宣称停止监听能撤回已接收消息，也不把权威resolve当native idle。
- 检查后生产代码未再改；只新增发送器回执已赢得race、await尚未恢复时取消的回归。最终29文件摘要为`f3384f12d370fb6d181c0b1637968ce2c622c8bf0176e7c654d7ec825c248aa0`，全量运行和变异恢复均已核对同一身份。跨绑定历史未知仍阻止重开；发送前确定零调用可释放不存在的pending，但已预留intent不重投。
- 真实beta、两真人HTTP、两逐席MCP和两个本地脚本queue进程已形成公开→沉默链。脚本不是Codex生成；真实连续运行、第二原生AI、远程联机和UI未跑。B12/B14被策略拒绝的资源不重试清理。
- 验证输出保留在`artifacts/b15-managed-wake-session-20260831/`；检查输入29文件固定，已有失败轮次和退出日志均不覆盖。仍不暂存、提交、归档或部署。

## B14 历史直接证据

- 原任务「TokenGame 临时单席接入验证」一次MCP只读准备实际成功。随后3个不同B公开来源分别经一次queue触发1次真实start、1次resolve和1次成功公开，A没有补提示或点击；实际4次原任务输入、7次原任务游戏MCP调用，未用满12/4上限。
- 两例在等待区手数0，一例在进行中第1手；源事件到公开43.857/46.785/43.660秒，不能当纯推理耗时或实时性通过。第1例采样较迟只见历史，第2/3例两页看到同文AI座位气泡。原任务本地上下文为gpt-5.6-sol/max，未覆盖，不当作上游模型身份认证。
- 第3例跨街回复旧UI漏标，现只修`src/host/table-view-model.cjs`。真实producer回归先20项中5失败，再20/20；相邻45/45、旧mapper变异1条被杀。`b14_late_check`隔离上下文复核无新finding；修复后主线程脚本双页14/14，窄屏not_run，不追加原生模型调用或倒算原始显示。
- 本批权限已撤销、独立stdio返回`model_command_token_rejected`；该检查没有HTTP状态码，不写403。临时`H:/tokengold/.codex/config.toml`已按身份移除，两页关闭；beta37068/控制器31624正常退出0、输出完整，52231无监听。18条事件、20条记录、footer和关闭回执同run_ref；驻留36分37.598秒不是模型时延或真人UAT。
- 清理未完整：`H:/tokengold/.codex/tokengame-b14-authorized-private-3914f461/a.json`已失效但仍在，宿主管理MCP PID35872在最后只读快照仍在。精确清理命令被工具策略拒绝、未执行，禁止换Node/另一工具删除或反复杀进程；不关闭其宿主父进程。B12失效文件未读/未动。下次只读确认状态，不能把旧PID永久认定为同一进程。
- 最小可Git复核事实包：`evidence/probes/b14-codex-queue-native-20260831/`，不是accepted架构/产品证据，未提交。原始实机证据：`artifacts/b14-native-readiness-authorized-20260831/`；局部修复、独立检查及脚本UI：`artifacts/b14-late-marker-fix-20260831/`。UI夹具两次已分别受控关闭，run-02为14项有效检查，不触碰前批策略阻塞。

## B14 第一批历史：具体AI权限确认前停止

- 已冻结370文件、80项既有脏路径与空暂存；B13七份最终源码身份匹配。启动一批独立回环服务，A建房落座，B停在公开范围确认；尚未Ready、开手或发言。
- 原任务「TokenGame 临时单席接入验证」只读状态为notLoaded，这不是实际MCP就绪证明。本批0原任务输入、0queue、0游戏MCP调用、0真实AI评估，未执行Node测试或变异重跑。
- AI权限勾选和下载连接文件被明确要求具体确认，后续DOM仍为未勾选、未绑定、下载禁用，下载等待对象未创建。未生成凭据或临时MCP配置；不以HTTP、终端、另一个浏览器重试。
- 等待用户明确允许：该原任务AI读取本席合成底牌及公共聊天、以该席AI身份公开发言、下载仅存本机的连接凭据；不含下注权限，每批结束撤销。确认后另开有界资源批次，先实际工具准备，再发queue；原12/4上限不是必须用完的配额。
- 两页已关闭，beta8200与控制器29452通过本批IPC正常退出0、非强制、输出完整，61334无监听；捕获仅header/footer、0个采纳事件，与唯一关闭回执同run_ref。本批9分43.677秒服务驻留不是模型或测试性能数据。空私有目录已删除，B12失效私有文件保持未动。
- 记录见 [B14权限边界](./REVIEW-LOG.md#b14-native-readiness-permission-boundary)，原始产物在 `artifacts/b14-native-readiness-20260831/`，被Git忽略。无产品源码修改、无提交、无自动化定时任务；长期测试授权不等于测试已在后台持续运行。

## B13 历史工作

- 已完成父子IPC关停与有界本地控制器；文件完整、写入ACK、关闭结果、输出流完整性与实际退出分别判断。只控制自身创建的子进程，不提供HTTP关停或模型关停权限。协议和使用方式见 `docs/AI-LIFECYCLE-RECEIPTS.md`。
- `b13_shutdown_check` 已完成独立上下文检查：关停中父断连、首因掩盖输出截断、晚到输出错误三类缺陷均有先失败后通过证据。最终四文件46/46、Node 10460.5787ms；没有未修复的已知本批缺陷，不代表全产品无缺陷。
- 主线程在最终代码上实跑两席HTTP脚本AI公开终态及关停，26/26、197.6686ms；隔离副本9条变异全部杀掉、0存活/未评估，370文件已逐项还原。591条仅静态核对；未跑全量、浏览器、PTY或真实模型。检查前38项及早先26项整合仍保留，不累加为最终独立覆盖。
- 实现分工两次因载体状态变为pending_init而停止，已逐次核对落盘修改。最后一次不重启实现，直接检查已保存代码；原因unknown，不归因框架。取证、输入身份和中断记录在 `artifacts/b13-host-readiness-shutdown-20260831/`。
- 宿主只读检查确认协议支持按任务查询MCP状态，但原任务实际读取通道尚未验证；全局reload没有单任务参数，未执行。08:26时未见旧匹配MCP；09:23时旧beta16608仍在且未动，失效私有文件仍在。时点快照不追认B12清理完整。
- 唯一本地裁决：[B13 审查](./REVIEW-LOG.md#b13-host-readiness-shutdown)。源码7文件与证据49文件清单、11条显式测试命令台账在本批目录；B9–B12的163份冻结证据逐项核对未变。下一步不再重做关停实现，先处理原任务实际MCP就绪。

## B12 实际结果与停止边界

- DEC-20260831-001只使用2/3次输入，14.829秒和8.535秒，原任务都停在工具发现；0游戏MCP调用、0queue、0Ready/手牌。剩余额度不结转，不继续重试。
- 同一私有文件经独立stdio客户端读取成功；CLI在原任务cwd也能读到该MCP配置。这只把问题缩到原生任务的新工具发现/加载，确切根因unknown，不证明扑克或模型能力失败。
- B10旧原始工具输出本次可读：一次ai.start、一次ai.resolve，权威返回hand_advanced、1→2手。旧回答因跨手被丢弃，而不是silent；B10当时的冻结产物保留，不倒写历史。
- 本轮没有改产品源码。B11最终12文件、B11的86份冻结证据及B10实机13份证据已重新核对一致；没有重跑全量或变异测试。
- 真人撤销后旧权限HTTP返回403/model_command_token_rejected；配置已删除、浏览器和本轮beta45800/64300已停止，旧beta16608未动。
- 清理为partial：`H:/tokengold/.codex/tokengame-b12-private-lpTGXX/a.json`已失效，但删除命令被工具策略拒绝，未改用其他方式绕过。宿主管理的匹配MCP进程仍可见，不能反复杀进程或重启整个Codex影响其他任务。
- PTY中Ctrl+C后捕获仅有首行、stderr为空，没有footer/关闭回执；离线工具原生退出2、partial。不能把外层shell退出1或端口消失当成记录器已成功关闭。

## B11 本次完成与验证

- 新增默认关闭的本地AI事件记录器；只在本次回环beta的进程内权威启用。独占新建、不覆盖旧证据，去除聊天、手牌、凭据和自由格式ID，有限文件和待写队列。
- 离线工具关联来源→评估开始→实际终态，分辨公开、沉默、丢弃、回收与未观察到终态。文件内容、write成功回执、close成功回执分开；离线退出0不代表运行清理或真实AI通过。
- Primary直接反例与新上下文复核修复完整性/时差误报、跨席身份冲突、错误码扫描与旧变异失配。最初失败全部保留，没有通过删断言或放宽规则做绿。
- 最终全量1110/1110、58.991秒（过滤默认端口1项）；该默认路径在确认7802空闲后另跑7/7、1.384秒。两者有重复测试，不合成1117项独立覆盖。
- 本批20条变异及相邻17条全部杀掉、0存活/未评估；全仓585个锚点仅静态核对可达，不声称全量变异门禁重跑。两席连接浏览器51/51、6.467秒，5张最终截图已查看。
- 不新增模型、真实queue或游戏任务消息；不动旧beta16608，不修改B9/B10冻结证据，不重跑四真人或13手验收。原始回执位于 `artifacts/b11-ai-receipts-20260831/`，仍由Git忽略。
- 本批唯一裁决：[B11 审查](./REVIEW-LOG.md#b11-ai-lifecycle-receipts)。启用与解读：[本地回执说明](./docs/AI-LIFECYCLE-RECEIPTS.md)。只完成本地取证，不是性能优化或主动AI能力交付。

## B10 本次实机结果

以下为当时的取证记录；其中“终态unknown”现由B12的独立历史补证更新为hand_advanced丢弃，不改写旧产物。

- 用户明确说“同意验证”；复用原测试任务，3次任务输入：连接只读准备、一次自动queue、撤销后只读检查。
- B经真实页面发公开消息后，A没有新点击或提示；原任务自动启动一次回合，耗时49.009秒。
- 来源事件后约20.667秒首次观察到THINKING，33.261秒首次观察到第2手，38.867秒首次观察到IDLE；AI公开消息0。以上是700ms轮询的首次观察，不是精确内部耗时。
- 宿主读取接口返回的3个回合均无items；精确start/resolve次数、合法silent/丢弃终态和实际模型/强度unknown。现象与跨手丢弃相符，但不能把推断写成直接证据。
- 真人撤销成功；原生只读检查完成但回执同样不可见。另以原令牌做一次只读HTTP检查，明确返回403/model_command_token_rejected；两者不混为一条证据。
- 临时配置、私有连接文件、MCP、服务和浏览器已清理，旧beta16608保留；窗口已结束，无额外调用授权。运行代码及历史证据身份未变，未重跑既有测试。

## B10 先前本地准备

- 只新增默认关闭的测试支持探针及测试、变异规格，不接入默认启动或修改产品能力声明。
- 对指定席位的新公开真人事件最多领取一次、最多通知同一任务一次；没有第二套模型或扑克操作权限。
- 独立复核修正Windows路径和已观察到MCP故障后仍可能通知的两处缺陷，并补平台分支可达性测试。
- 最终117/117在52文件哈希核验副本中通过（Node 4787.5109ms）；既有相邻回归89/89（5340.5689ms）；
  8条变异全部杀掉、0存活/未评估（整轮23179.1827ms）。初版失败、红测及重复运行次数在审查记录中分列。
- 测试确实使用本地HTTP、逐席MCP和新接收进程，但AI发言仍由脚本产生；没有真实queue、任务输入或模型调用。
- B9成功样本复算为玩家事件到权威公开33.460秒，公开后收尾11.556秒，公开晚于该次行动截止3.822秒。
  这不是新性能测试；纯推理时间与浏览器首次显示时间unknown，不能宣称达到实时要求。
- 48个产品运行文件与25份B9原始证据身份未变，测试副本已清理，宿主配置没有恢复，旧beta仍未动。

## B9 实际新增结果

- 用户明确同意后，只创建一个隔离游戏任务，五轮输入、九次游戏MCP调用，没有第二套模型API。
- 使用本席授权文件，经Codex Desktop原生模型读取自己的牌和公共聊天，真实生成发言并显示到两页的座位AI气泡。
- 两次生成中，一次44.917秒整轮后成功发布，另一次62.996秒后因跨手被丢弃；没有暂停或延长扑克行动时钟。
- 真人先撤销，再让同一任务使用原文件调用，得到 `model_command_token_rejected`；真人打牌仍可继续。
- 修复首次建房长邀请码挤出复制按钮；390/320px复制前后新增16项，定向连接UI共51/51、6,321ms；
  定向Node20/20、1,364.056ms，七份语义合同匹配。失败与修复记录分开保留。
- 临时配置、连接文件、三个MCP子进程和本轮服务/浏览器已清理；保留闲置的游戏验证任务供复核。
  旧beta未动，没有提交或部署。

## 已完成的本地基础：B8

起点为 `main@bbdcf2b`，B6单一协调器与B7本地启动入口已提交，接手时工作树干净。
源码检查发现：原共享模型令牌能触及多席，模型评估也没有获得自己的底牌。B8补齐：

- 真人在牌桌明确授权，下载只属于本席的AI连接文件；两席互相隔离。
- MCP从私有文件读取权限；模型只能读取获准局面和公开发言，不能替真人下注。
- 权威在 `ai.start` 成功时提供本席底牌、公开牌面与最近公共聊天，其他席未公开底牌不进入返回。
- 可换发、撤销；刷新/短断保留连接，离桌/到期撤销。已交权威的在途请求不保证回滚，旧响应不能重新带回私有上下文。
- 启动入口、界面、插件说明和测试同步更新。未改扑克规则、L0–L2或公开交流规则。

## 已有事实与验证边界

B8基线875/875；最终全量测试925/925。两浏览器＋两MCP进程连接验收35/35；四浏览器多手验收
209/209，到第13手，控制台与意外网络错误0。最终557条变异全部杀掉，0存活/未评估，GATE=PASS；首轮失败已保留。
运行次数、耗时、失败原因与产物路径均在B8审查记录中，不把失败轮次合并成一次全绿。

B8浏览器实际用了Chromium、HTTP和MCP stdio；发言由固定脚本文字生成，只证明本地权限和交互链路。
该批验证副本来自 `git archive bbdcf2b` 加逐文件校验的改动，不是新提交。本轮没有重复执行其全量门禁。

## 下一步建议

B23本地合同、B24相对配置迁移、B25失败定位/本地修复、B26真实绝对配置恢复、B27第二次重启后工具直接恢复和B28空闲任务交接合同均已闭合。下一步不是继续重写sender、页面、说明或传输，也不是再次迁移配置或重启。`npm run codex:play -- "<当前 Codex 项目根绝对路径>"`稳定启动回环beta后，应结束该回复，让游戏任务空闲，再由真人到Web牌桌操作。

B27这一次最多1次通知已实际消耗，且固定目标是正在执行开发的同一任务，结果为1/1/0；不得沿用该窗口或重投。B28没有新增通知额度消费。后续本地朋友组合应使用各玩家已结束启动回复的空闲游戏任务，再按逐席授权、本人开窗、权威start/唯一terminal、页面公开或silent事实及清理分项验收。本人勾选不是宿主遥测。B15的跨窗口未决/清理围栏、B20历史精确码unknown、B22 source/首项工具顺序unknown与七份失效下载手工删除边界均保留。

`DEC-20260901-002`现已执行完毕，不扩到额外重启、远程监听、第二模型、模型/推理覆盖、扑克时钟修改、全局安装、提交或部署。B28没有原生样本，B27仍未取得权威终态，故`proactive_wake_verified=false`，`TG-EU-PROACTIVE-WAKE-SPIKE`与`TG-EU-PLAYABILITY-GATE`继续开放。

DEC-20260830-003的实机窗口已经得到同意、执行并清理，3次任务输入和1次queue额度均已使用。
后续同范围测试依据DEC-20260831-002持续授权，不沿用已关闭的B10/B12窗口余量；第一次相对迁移/重启由DEC-20260901-001执行，第二次绝对配置写入、手动重启与工具核对已由DEC-20260901-002/B27执行。B27的最多1次通知额度已经消耗，不是可重复调用授权；B28只消费本地测试。
原任务为 `01a052c9-5259-7a61-b26f-35731734994e`（`TokenGame 临时单席接入验证`），保留闲置且无有效游戏权限。B12窗口也已停止，不因第三次未使用而留下追加授权。

B9显式消息、B10跨手丢弃、B12工具发现失败各保留原批次身份。B14是新的直接因果链证据，不拼接旧样本；Codex Gate5仅本批限定范围pass，Gate9 blocked，仍不交付完整主动功能。
Claude仍not_run、点击/新提示要求unknown。没有无限后台模型调用授权。
完整插件入口、游戏自由输入自动公开、内嵌UI、第二真实席与Claude均还未验证。
Claude Desktop / Cowork安装情况unknown。旧CLI前缀/Hook/补交证据只按旧路径成立。

短期“朋友异地建房试玩”还有远端认证/连接与部署、四真人45分钟试玩两个交付缺口。
目前 `npm run beta` 仅回环监听，同一台电脑多浏览器能验证；不能把该地址发给异地朋友当成可用服务器。
Gate 5若实测失败且影响已确认主动交流结果，必须回语义对齐，不能自行把被动轮询写成等价交付。

## 接续与Git边界

- 当前恢复点：[PROJECT-PLAN-TREE.md](./PROJECT-PLAN-TREE.md#当前恢复点)。协议与P1–P7见 [TAKEOVER-PLAN.md](./TAKEOVER-PLAN.md)。
- 执行 `npm test`、`npm run gate` 可复跑单元/集成与全部变异；**变异会临时改产品源码，须独占运行**。
- 浏览器驱动为 `test-support/model-binding-browser-acceptance.mjs` 和 `test-support/table-web-acceptance.mjs`；浏览器依赖需按本机环境提供。
- B9证据在 `artifacts/b9-real-host-20260830/` 与 `artifacts/b9-invite-fix-20260830/`；25文件摘要及48文件运行身份见B9审查记录。原始证据由Git忽略，不把本地路径称为已提交产物。
- B10证据在 `artifacts/b10-wake-probe-20260830/`；23文件清单与摘要见 `evidence-identities.json`。
  探针的无副作用默认入口是 `node test-support/codex-queue-wake-probe.cjs`；未经窗口授权不得加 `--live`。
- 本次实机证据另存 `artifacts/b10-real-wake-20260830/`：任务收据、1200份有上限的双席投影采样、截图、撤销与清理记录；仍由Git忽略，受控审查记录绑定文件摘要。采样达到上限发生在回合完成之后，不能用其缺失尾段证明清理；清理有独立回执。
- B11本地证据及源码清单见 `artifacts/b11-ai-receipts-20260831/final-source-identity.json`、`evidence-identities.json`。保留两个隔离验证副本便于复核；它们不是新提交，不含宿主配置或真人秘密。验证子进程/私有下载目录已清理，旧beta保留。
- B13本地证据见 `artifacts/b13-host-readiness-shutdown-20260831/evidence-identities.json`、`verification-ledger.json` 和 `final-source-identity.json`。保留隔离变异副本及其还原回执；不把49份证据的数量当测试数量，不把脚本AI约0.2秒的整合耗时当模型延迟。
- B14原生事实包的manifest已由隔离检查逐项复算；原始记录与修复后UI使用不同源码身份。当前受审四文件见`artifacts/b14-late-marker-fix-20260831/check/input-files.json`，不要把旧原生mapper哈希改写成修复后版本。
- B20去敏原始回执在`artifacts/b20-hand-active-managed-wake-20260901/authority-lifecycle.jsonl`，受Git忽略；受控事实包在`.trellis/tasks/08-26-public-ai-table-talk/research/b20-hand-active-managed-wake-diagnostic-20260901.md`。0 turn的完整文件不是AI成功证据。
- 本地收口策略为 `manual_closeout`：未暂存、未提交、未推送；检查 `git status` 时须同时查看新增未跟踪文件，普通 `git diff` 不显示它们。`artifacts/`在Git忽略中，证据摘要写在受控文档里。
- 不归档整个 `.trellis/tasks/08-26-public-ai-table-talk`，不声称完整MVP或真人验收已经完成。
- 未修改冻结的B6只读观察包 `H:/dual-observation-inbox/tokengame-b6-1-b6-2-20260830-161331.md`。

历史B8/B9收尾时发现旧beta进程PID16608仍在运行：2026-08-30 16:37:36由Claude的bash父进程6720启动，
早于本轮接手。没有终止或重启它，避免丢失其内存牌局；它不属于本轮临时验收实例，也不是新版后端证据。
后续执行 `npm run beta` 时使用新输出的地址；不要拿旧窗口作为B8验收入口。
B14启动前已未观察到16608，消失原因unknown，不声称本轮将它关闭。

{
  "name": "内测入口与模型面可见兜底",
  "note": "B7 兜底回归随 B8 迁移：内部空托管仍有等待说明；外部通道必须逐席绑定。原令牌随机/不打印的否证迁移到本人下载函数，不保留找不到的旧生成器锚点。所有14项仍必须被评估。",
  "test": "test/model-liveness-fallback.test.cjs",
  "mutations": [
    {
      "id": "empty-hand-says-nothing",
      "file": "src/host/model-command-surface.cjs",
      "find": "          ...(intents.length === 0 ? emptyHandedReason(handles.length) : {}),",
      "replace": "",
      "note": "空手而归时不说在等什么。这是缺陷本体：模型读到的两种处境一样，于是它只能继续轮询，而一席都没绑的那种轮询到世界末日都不会变。宿主那边看起来是「AI 在等」。"
    },
    {
      "id": "no-seat-looks-like-idle",
      "file": "src/host/model-command-surface.cjs",
      "find": "  if (seatsPolled === 0) {\n    return {\n      waiting_on: \"human_entry\",",
      "replace": "  if (false) {\n    return {\n      waiting_on: \"human_entry\",",
      "note": "一席都没绑也报「继续轮询」。比不报更坏：它明确地说了一句错话——「不需要真人做任何事」，而实际上非要真人去入座不可。"
    },
    {
      "id": "idle-looks-like-no-seat",
      "file": "src/host/model-command-surface.cjs",
      "find": "  return {\n    waiting_on: \"table\",",
      "replace": "  return {\n    waiting_on: \"human_entry\",",
      "note": "反向：席位在、只是没待办，却告诉模型去叫人入座。人会照着去建一间已经建好的房，或以为自己那一席掉了。"
    },
    {
      "id": "waiting-reported-with-intents",
      "file": "src/host/model-command-surface.cjs",
      "find": "          ...(intents.length === 0 ? emptyHandedReason(handles.length) : {}),",
      "replace": "          ...emptyHandedReason(handles.length),",
      "note": "有待办时也报在等待。模型在同一份回答里读到「这是你的待办」和「本席此刻没有该说话的意图」，两句互相矛盾，而它只能挑一句信。"
    },
    {
      "id": "next-step-emptied",
      "file": "src/host/model-command-surface.cjs",
      "find": "      next_step: \"本机协调器上还没有任何席位。请真人在浏览器里打开牌桌，建房或用邀请码加入；\"\n        + \"入座之后本席才会有待办。在那之前继续轮询不会有变化。\",",
      "replace": "      next_step: \"\",",
      "note": "只报机器可判定的枚举，不给人话。模型自己解决不了「去浏览器里坐下」，它能做的就是把这句话转达——空字符串等于它无话可转。"
    },
    {
      "id": "empty-round-counted-as-drive-error",
      "file": "src/host/table-web-host.cjs",
      "find": "      for (const failure of claim.body?.result?.failures ?? []) {",
      "replace": "      for (const failure of claim.body?.result?.waiting_on === undefined ? [] : [{ code: claim.body.result.waiting_on }]) {",
      "note": "把「在等」记成驱动故障。一张还没有人入座的桌子会每个驱动周期积一条错误，而 driveErrors 是诊断真故障的唯一入口，被这种噪声填满就等于没有。"
    },
    {
      "id": "beta-route-left-disabled",
      "file": "src/run-beta.cjs",
      "test": "test/beta-entry.test.cjs",
      "find": "      modelBindingEnabled: true,",
      "replace": "      modelBindingEnabled: false,",
      "note": "内测入口生成了令牌却没接上协调器。这正是 `npm run web` 的现状：牌桌照常能玩，模型路由关着，而它关着的方式是安静的——没有任何一席的宿主 AI 能发言，而 /api/health 里那行 disabled 不会有人主动去看。"
    },
    {
      "id": "beta-token-hardcoded",
      "file": "src/host/table-web-host.cjs",
      "test": "test/beta-entry.test.cjs",
      "find": "      token: this.custody.remember(`model-token-${crypto.randomBytes(32).toString(\"base64url\")}`),",
      "replace": "      token: this.custody.remember(\"tokengame-local-beta-token\"),",
      "note": "逐席下载仍不可使用写死的默认令牌，真实 beta 下载行为必须杀掉这个变异。"
    },
    {
      "id": "beta-token-not-random",
      "file": "src/host/table-web-host.cjs",
      "test": "test/beta-entry.test.cjs",
      "find": "      token: this.custody.remember(`model-token-${crypto.randomBytes(32).toString(\"base64url\")}`),",
      "replace": "      token: this.custody.remember(crypto.createHash(\"sha256\").update(\"tokengame\").digest(\"hex\")),",
      "note": "看起来随机但为固定散列，两次真实 beta 下载的令牌相同，必须失败。"
    },
    {
      "id": "beta-token-printed",
      "file": "src/host/table-web-host.cjs",
      "test": "test/beta-entry.test.cjs",
      "find": "    this.modelBindings.set(session.seat_handle, binding);",
      "replace": "    process.stdout.write(binding.token + \"\\n\");\n    this.modelBindings.set(session.seat_handle, binding);",
      "note": "本人绑定时把 token 印到 beta 终端，真实下载后扫描整个 stdout/stderr 必须发现它。"
    },
    {
      "id": "beta-claims-proactive-wake",
      "file": "src/run-beta.cjs",
      "test": "test/beta-entry.test.cjs",
      "find": "    proactive_wake_verified: false,",
      "replace": "    proactive_wake_verified: true,",
      "note": "声称持续主动产品已验证。B14只证明固定版本单席有限样本，不能把该字段翻成完整产品能力。"
    },
    {
      "id": "beta-drops-polling-fallback",
      "file": "src/run-beta.cjs",
      "test": "test/beta-entry.test.cjs",
      "find": "      : \"有界自动通知默认关闭；当前可由宿主轮询领取 ai.take_intents。\",",
      "replace": "      : \"有界自动通知默认关闭。\",",
      "note": "不说兜底是什么。缺主动唤醒时「不静默卡住」的全部内容就是这句话——去掉它，人会以为 AI 会自己醒过来，然后等一个不会到的东西。"
    },
    {
      "id": "beta-hides-token-path",
      "file": "src/run-beta.cjs",
      "test": "test/beta-entry.test.cjs",
      "find": "      process.stdout.write(joinInstructions({",
      "replace": "      void (joinInstructions({",
      "note": "只打机器启动行，不告诉人逐席授权/下载与配置路径。保留旧 id 以便追溯，检查对象已迁移到 B8 本人下载说明。"
    },
    {
      "id": "beta-falls-back-to-random-port",
      "file": "src/run-beta.cjs",
      "test": "test/beta-entry.test.cjs",
      "find": "  const webPort = readPort(env.TOKENGAME_WEB_PORT, \"TOKENGAME_WEB_PORT\", DEFAULT_TABLE_PORT);",
      "replace": "  const webPort = readPort(env.TOKENGAME_WEB_PORT, \"TOKENGAME_WEB_PORT\");",
      "note": "回落到随机端口。人刚把地址填进宿主配置，重启一次就指向了上一个端口，表现是「模型说连不上牌桌，而牌桌明明开着」。"
    }
  ],
  "excluded": []
}

# 参与 TokenGame 开发

感谢你愿意参与。TokenGame 目前是本地 Alpha，不是已经稳定的线上牌室。我们优先接受能让“朋友建房打牌，
每个座位连接自己的会话 AI 并公开交流”更接近真实试玩的改动。

## 开始之前

- 使用 Node.js 22 或更高版本；当前没有第三方运行时依赖。
- 先阅读 [README.md](./README.md) 的当前能力边界，以及 [STATUS.md](./STATUS.md) 的真实恢复点。
- 小型缺陷、测试和文档修正可以直接提 PR。涉及牌局规则、权限模型、宿主合同或产品范围的改动，请先开
  issue 说明场景、风险和验收方法，避免实现正确但方向不一致。
- 当前公开协作目标是朋友私人房 Alpha。大厅、匹配、信用体系、AI 托管下注和真钱玩法不在近期范围。

## 本地开发

```powershell
npm test
npm run beta
```

`npm test` 是串行 Node 测试。`npm run gate` 还会运行完整变异门禁，耗时更长，并会在受控流程中临时改写
源码；不要与其他修改或测试并行运行。涉及页面的改动还应运行对应的 `test-support/*browser-acceptance.mjs`
验收，并检查桌面与窄屏截图、控制台错误和资源清理。

只想查看普通牌桌可以运行 `npm run web`。连接 Codex 的本机流程见 README；不要为了测试把服务改成公网
监听，也不要提交真实连接文件。

## 不可破坏的边界

- 服务端是牌局、时钟、底牌和 AI 生命周期的唯一权威；浏览器与模型不能自行判定结果。
- 真人负责下注、Ready、亮牌和授权。AI 只能读取本席获准上下文并公开发言或沉默。
- 模型不能获得或回传席位凭据、恢复凭据、真人会话令牌，也不能用自己的参数选择别的席位。
- 每位查看者只能看到自己有权看到的底牌；公开时间线不能成为私密数据旁路。
- queue 已接收不等于模型已启动，模型回合完成也不等于权威已结清；测试和文档必须区分这些事实。
- 新宿主适配器应遵循共享合同，不复制一套扑克权威或悄悄降低已确认的公开交流语义。

## 提交与 PR

1. 从最新默认分支创建短生命周期分支。
2. 保持改动聚焦，并为行为变化加入能先失败、修复后通过的测试。
3. 至少运行受影响的聚焦测试和 `npm test`；没有运行的长门禁要在 PR 中明确写出。
4. PR 说明应包含：用户场景、根因或设计理由、实现边界、验证命令与结果、仍未解决的问题。
5. 不要把脚本替身、浏览器夹具或单席探针描述成真实多人 AI 验收。

建议使用清晰的 Conventional Commit 风格，例如：

```text
feat: add per-seat host connection recovery
fix: reject stale AI turns after hand advance
test: cover side-pot split across disconnected seats
docs: clarify local-only alpha setup
```

## 敏感信息与本地产物

绝不要提交：

- `tokengame-ai-*.json` 或 `.tokengame-private/`；
- API key、访问令牌、Cookies、真实聊天或未脱敏的宿主输出；
- 本机绝对路径、任务 UUID、进程转储和浏览器 profile；
- `.playwright-cli/`、`output/`、`artifacts/`、`test-results/` 等运行产物。

若证据对复现缺陷确有必要，请只保留最小、去敏、可审查的文本事实，并在 PR 中说明它证明什么、不证明
什么。发现安全问题时不要公开粘贴凭据或利用细节；请先通过仓库维护者提供的私密渠道报告。

## 使用 AI 辅助贡献

欢迎使用 Codex、Claude 或其他工具，但贡献者仍对代码、许可证来源和测试结论负责。PR 中应说明哪些关键
验证实际运行过；不要把模型自述当作测试结果，也不要提交未经审阅的大段生成代码。

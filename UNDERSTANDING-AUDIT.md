# Codex 接手理解审计（2026-08-30）

本记录由当前 Primary 自行检查源码与运行验证形成，不是 Claude 的转述，也不是独立外部审查。项目现行理解入口为 `STATUS.md#project_intelligence`；本文件记录接手依据，不建立第二棵路线树。

以下事实是接手时的基线快照；本批后续实现与最终925测试/557变异/浏览器结果以
`REVIEW-LOG.md#b8-seat-model-binding` 为准，不把本文件中的“待修复”当作当前缺口。
后续 B9 已验证当前 Codex Desktop 的单席显式生成与撤销，见 `REVIEW-LOG.md#b9-real-host-seat-probe`；
下文仍为接手基线，不作为“当前尚无任何实机接入”的依据。主动唤醒、异地联机与真人内测仍未完成。

## 结论与范围

- 用户当前授权：“现在分析项目现状，由你接手开发”。这是接替 Primary 开发，不是顾问试用；承接已确认的私人房 MVP，不重开 L0–L2 语义讨论。
- 检查起点：`main@bbdcf2b1c4968fcace96fcc1cc69f97e57c4e18b`，工作树和暂存区均干净。B6-1、B6-2、B7 都已提交。
- 独立基线：本次 `npm test` 实际 exit 0，875/875，fail/skipped/cancelled/todo 均 0，Node 报告 47038.9874 ms。尚未把历史变异或浏览器结果算作本次验证。
- `understanding_depth: takeover_ready`，仅授权同一合同内的本地实现与验证；不授权发布、安装宿主插件、修改全局配置、真实模型外部调用或真人验收签字。

## 自行重建的端到端地图

真人浏览器创建/加入 → `TableWebHost` 保存 Web 会话 → 唯一 `SeatCustody` 保管席位凭据 → `CommandSurface` 验证身份 → `RoomStore`/`HoldemHand`/`SeatAiStore` 裁决 → 逐席视图与公共时间线 → 浏览器气泡/手动动作。

AI 路径是当前宿主 MCP → `/api/model/command` → 同一协调器的 `ModelCommandSurface` → 带凭据的 `ai.take_intents/start/resolve`。只有这三条 AI 命令能发布座位 AI 话术；它们不能提交筹码动作。进程内脚本适配器也走同一模型命令实现，但其成功不是真实宿主证明。

## 四个理解平面

1. **价值/产品**：标准德扑是底座，真人与自己的会话 AI 在座位旁公开互动才是差异。近期目标是朋友私人房内测；大厅、匹配、信用、市场和 AI 自主下注均不进入本批。
2. **设计/不变量**：权威与宿主分离；一席一 AI；每席只读自己获准的私有信息；普通宿主内容不自动公开；自然语言没有扑克动作效力；延迟/断线不暂停权威计时。浏览器和模型不能持有可跨进程恢复席位的核心凭据。
3. **当前现实**：单栈浏览器、标准扑克、房间/恢复、AI 事件/配额以及共享命令面均已实现。B6 消除了 MCP 空托管，B7 增加一键本地启动，但模型接入仍是进程级令牌；多人打开同一协调器时，一个模型能领取其他席位的待办。模型上下文也只有触发事件，`ai.start` 未返回该席底牌和完整局面。875 项全绿没有覆盖这两个产品断点。
4. **候选/未知/历史**：旧 Codex CLI 桥探针只证明旧显式前缀/Hook/补交回路；新私人房、Codex Desktop/Cowork 内嵌 UI、无点击唤醒、四真人 45 分钟 UAT 均仍未证实。13 手浏览器证据使用脚本模型，不是四个真实 AI。中立远端服务/安全联网仍是后续边界，本批保持回环。

## 地图外抽查（非只读交接清单）

| 模块 | 直接检查及其对接手的作用 |
| --- | --- |
| `src/host/core-client.cjs` | 核实 HTTP 与进程内传输有相同 dispatch 形状，席位凭据不是客户端缓存；本批必须同时验证两条路径。 |
| `src/authority/due-work.cjs` | 核实行动截止、席位释放、评估/claim 回收和 pending 促进由权威计时器执行；不能为接模型而改这些时序。 |
| `src/shared/http.cjs` | 核实 JSON 大小限制、no-store 与服务关闭；新的私有绑定响应沿用这些出口。 |
| `src/authority/command-server.cjs` | 核实回环限制、先令牌后版本门，以及非浏览器访问设计；本批不放开远端监听。 |
| `src/authority/host-surface.cjs` | 核实真人/模型命令分权；其“底牌随 intent 裁剪提供”注释与实际 payload 不一致，不能把注释当能力证据。 |

## 反向挑战与下一步

“每人各跑一个协调器，所以进程令牌等于本人”是未由入口强制的假设：B7 恰好指导多浏览器连接同一服务。仅补说明不够。反过来，把 `view.hand` 放进模型白名单也不够：在多席共享令牌下会扩大私有信息访问。本批先做人确认的逐席绑定，再由成功的权威 `ai.start` 返回同席上下文；具体协议与验收见 `TAKEOVER-PLAN.md`。

保留风险：同一个 OS 账户下有任意文件/进程权限的宿主并非安全沙箱；本地能力令牌不能证明模型来源。绑定只能隔离正常插件调用，不能宣传为反作弊证明。真实宿主安装与主动唤醒仍需后续单独实测。

## 模型与治理边界

- 当前载体为 Codex Desktop；精确 provider/model/configuration 未独立验证，记为 `unknown`，不凭品牌声称不同模型独立性。
- 保留当前 Adaptive、medium、本地原型边界；不调整框架或 runtime profile，不默认启用更高风险权限。
- `still_unreliable_for`：真实宿主唤醒、其他人 OS/网络环境、长时真人可玩性、生产认证与部署安全。
- 未获得自动本地提交授权；保持 `manual_closeout`，以可检查 diff 交付，不 push。
- 旧理解投影与 CLI 收据保持历史范围；本批不生成/提升新的 L0–L2 投影，不以旧收据冒充当前 B8 完成。

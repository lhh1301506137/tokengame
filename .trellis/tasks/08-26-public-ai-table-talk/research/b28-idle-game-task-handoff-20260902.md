# B28 空闲游戏任务交接合同事实包（2026-09-02）

## 问题与范围

- B27 已证明绝对 `cwd` 配置、手动重启和项目工具加载均恢复；唯一通知固定到仍在执行开发回复的同一任务后，只达到尝试/接收/权威结清 `1/1/0`。现有证据不能确认同一活动任务的精确排队规则，但已经足以否定“启动 beta 后让同一回复持续看守页面，同时期待并发模型回合”的操作假设。
- B28 不重写通知传输、不新增调度器，也不创建任务或触发原生模型。它只把空闲载体前提写进入口横幅、Web 本人确认、项目 Skill、插件与操作文档，并用现有本地夹具验证这些表述不会回退。
- 本批沿用既有本地开发与长期测试授权；实际消费为 0 次通知、0 次 queue、0 个原生模型回合、0 次权威评估。没有修改模型、推理强度、扑克时钟、宿主全局配置或远程监听。

## 实现事实

- `src/run-beta.cjs` 在 managed sender 可用时明确提示：先让固定目标游戏任务结束当前回复并保持空闲；任务仍在运行时，通知可能已接收但不能并发结清。
- `web/table/index.html` 与 `web/table/table.js` 的固定目标说明采用同一边界；开启窗口的本人确认现在同时声明“目标游戏任务已结束当前回复并可接收新回合”。这是一项真人声明，不是宿主空闲遥测或技术门禁。
- `plugins/tokengame/skills/tokengame/SKILL.md` 明确要求启动回复结束后把任务交还为空闲，不在同一回合轮询、等待对手或继续开发；queue 已接收不等于模型已开始，更不等于权威已结清。
- 根 README、插件 README 与 `docs/MANAGED-WAKE-SESSION.md` 同步了相同操作顺序。实现没有宣称能检测任务是否真的空闲，也没有翻转 `proactive_wake_verified=false`。
- 新增/扩展三类回归：真实 beta 子进程启动横幅、Skill/插件/操作文档一致性、固定目标浏览器说明与本人确认。浏览器检查数由历史 44 项增为本批 46 项。

## 验证记录

- 五个相关 JavaScript 文件及两份测试脚本的 `node --check` 通过。
- Skill 的普通校验命令首次因 Windows Python 按 GBK 读取 UTF-8 文件而失败；使用同一校验器加 `python -X utf8` 后得到 `Skill is valid!`。这是载体编码故障，不是 Skill 内容失败，也没有改用另一套校验规则。
- 聚焦 Node 首轮 19 项中 1 项失败：插件 README 使用“回复结束后”，测试要求精确出现“结束……回复”；修正为一致的人话表述后，最终 `19/19` 通过，耗时 `1833.6463 ms`。首轮失败未被合并成全绿结果。
- 脚本浏览器 `46/46` 通过，`duration_ms=20729.272`；固定目标桌面与 320 px 页面已由 Primary 直接查看，说明、确认与控件顺序可见，没有横向溢出。报告记录 0 个浏览器错误，四个上下文、浏览器和夹具共 6 项清理均通过。
- 完整 `npm test` 最终 `1356/1356` 通过，0 fail/cancelled/skipped/todo，`duration_ms=79933.5359`。本批没有运行变异门禁；项目没有 lint 或 type-check 脚本。
- 持久化 B28 恢复文档后再次运行会读取 Skill/README 的同一聚焦套件，仍为 `19/19`，`duration_ms=1705.2711`；UTF-8 Skill 校验也再次通过。这次收尾复核不冒充新增产品覆盖。
- `git diff --check` 仅报告既有 `plugin.json` 的 CRLF→LF 提示，没有空白错误。最终活动连接槽不存在，7802 监听数为 0。浏览器证据保存在仓库外 `H:/tokengold/.codex/b28-idle-task-guidance-20260902-a1/`，未把本机证据目录写入产品输出。

## 裁决与恢复点

B28 裁决：`idle_game_task_handoff_guidance_implemented_and_locally_verified`。

本批关闭的是“入口与操作文档未表达空闲任务前提”的本地产品缺口，不关闭真实朋友组合、主动唤醒或实时性。页面确认不能替代宿主遥测；未来真正验收仍须使用已经结束启动回复并保持空闲的游戏任务，以权威 `ai.start` 和唯一 terminal 作为结果证据，不以 queue 接收数判断成功。当前不创建新任务、不自动重启宿主、不触发第二模型，也不开放异地监听。

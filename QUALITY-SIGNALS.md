# 质量信号

本文件记录会影响开发策略的重复性流程问题、验证失败和质量趋势，不记录一次性的普通错误。

## 当前状态

- 当前集成验证基线为 `npm test` 23 项通过，其中原有 Codex 桥接 11 项全部保留；四玩家 Playwright 三场流程控制台/页面错误为 0；真实 Codex 0.145.0 插件宿主探针仍为 `pass_with_notes`。
- 普通 Prompt/Stop 零桥流量、提示预公开、回答配对、幂等、截止时间、失败关闭、PreToolUse、Stop 重入保护、MCP stdio 与显式回答补交均有可重复断言。
- 浏览器验收发现的只读超时结算分叉和真宿主发现的 Stop 重入覆盖均已修复并加入回归测试，目前没有同根功能失败复发。
- 插件清单显式 `hooks` 字段能被真实宿主加载，但当前 plugin-creator 校验器误报未知字段；发布前应等待校验器修复或采用与宿主版本一致的验证器，不能删除运行时必需字段来换取假绿。
- 理解产物校验器在 v4 外部上下文模式下不能同时接收任何旧式 `--expected-*` 参数。R4 在移除全部该类参数后通过；后续固定使用“外部上下文路径 + 独立上下文哈希 + 项目根”的命令形态。
- 宿主探针结束后插件、测试 marketplace、专用信任配置、缓存、数据、端口和本次 MCP 子进程残留均为零。
- Trellis 0.5.8 已以 Codex 模式初始化；一次性 `00-bootstrap-guidelines` 任务已从现有 Node/Web/Hook 代码提炼并归档 6 份工程规范，模板占位项为零。
- 首个多人牌桌切片已实现独立扑克领域状态机、四身份桌级权威包装、逐玩家 HTTP/SSE 投影和可操作 Web UI；主池/两层边池、短额 all-in、奇数筹码、超时、非法动作、默认不亮牌和自愿亮牌均有回归。
- 本轮质量检查发现并一次修复重置幂等、all-in 跨街下注状态、进行中误重置、隐私提示与工程规范漂移，没有出现同根失败复发，当前不触发 Quality Sentinel 警报。
- 座位旁 AI 的独立检查发现并一次修复“非法 answer 误点亮全局阶段”和“事件计数完整但 DOM 截断到 80 条”两项一致性缺陷；现四视图覆盖 actor/request 配对负例、HTML 文本边界、桌面/窄屏几何及 87/87 完整事件历史，未出现同根复发。
- 最新 `npm run table` 启动、权威/桥健康、观察者零底牌和 `Ctrl+C` 后端口清零均直接验证。
- 下一质量门：用户体验接受该本地切片后再选后续路线；进入真实账号、远程多人、持久化或公网前必须新增生产级认证、并发、恢复、隐私与独立审查证据。
